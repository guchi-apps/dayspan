# DaySpan 固有ルール

このリポジトリで作業する Claude Code エージェント向けの DaySpan 固有ルールを記載する。

共通の開発標準・運用知識は `guchi-apps/docs` を一次情報源とし、このファイルには DaySpan 固有事項のみを置く。

## このリポジトリの構成（エージェント向けの前提）

| 項目 | 内容 |
|---|---|
| フレームワーク | Next.js 16（App Router）/ React 19 / TypeScript 5 |
| スタイリング | Tailwind CSS v4 + shadcn/ui（`components.json` の style は `radix-nova`） |
| ORM / DB | Prisma 6 + MariaDB（`app_dayspan`） |
| 認証 | Supabase Auth + Google OAuth（`@supabase/ssr`。ミドルウェアは `src/proxy.ts`） |
| パッケージマネージャ | pnpm 10 系（`packageManager` で固定） |
| 検証コマンド | `pnpm lint` / `pnpm typecheck` / `pnpm build`（`pnpm test` は lint + typecheck） |
| 開発サーバー | `pnpm dev`（既定ポート3000。`PORT` で変更可） |
| デプロイ | `main` への push で `deploy.yml` が VPS へ SSH デプロイ（PM2、プロセス名 `dayspan`、ポート3113） |

Prisma は 7 系ではなく **6 系** を使う。7 系は driver adapter が必須になり、他アプリ（issue-deck 等）の構成から外れるため。

## アプリ概要

DaySpan は、Google Calendar の予定と Notion のタスクを同じカレンダーUI上で統合して確認・操作するWebアプリ。

詳細仕様は `docs/spec.md` を一次情報源とする。

## データの一次情報源

- Google Calendar の予定本体: Google Calendar
- タスク本体: Notion
- DaySpan 固有設定・連携メタデータ: DaySpan の MariaDB

Google Calendar の予定本体および Notion タスク本体を、DaySpan DBへ恒久的に二重保存しない。

## 認証

- Supabase Auth + Google OAuth を使用する。
- NextAuth / Auth.js は使用しない。
- 初期リリースは許可されたユーザーのみ利用可能とする。
- 将来の一般公開を前提に、ユーザー単位で外部サービス連携を管理できる構造にする。

## 外部連携

- Google Calendar API: 複数カレンダーの取得、予定の作成・編集・削除
- Notion API: タスクDBとの同期、タスクの作成・編集・完了

UIコンポーネントから外部APIを直接操作する構造を避け、将来API/MCPから再利用できるサービス層を介する。

## 実装上の決定（変更する前に理由を確認すること）

コードのコメントにも書いているが、判断の理由が分かれていると再発しやすいものをここに集約する。

| 決定 | 理由 |
|---|---|
| Prisma は 6 系を使う | 7 系は `datasource url` が廃止され driver adapter が必須。他アプリの構成から外れる |
| `prisma.config.ts` の `loadEnv()` には `quiet: true` を付ける | dotenv v17は読み込み結果の案内文を **stdout** へ出す。`prisma migrate dev` / `migrate diff --script` が生成するSQLも同じstdoutへ流れるため、案内文が `migration.sql` の1行目へ混ざる。本番の `prisma migrate deploy` がその行で構文エラー（MariaDB 1064 → P3018）になり、さらに `_prisma_migrations` へ失敗が記録されて以後のデプロイがP3009で止まる（VPS上での `migrate resolve --rolled-back` が要る）。実例は `guchi-apps/aide-bot#10` |
| Notion は `taskDataSourceId` を一次キーにする | API 2025-09-03 以降、プロパティとクエリの対象はデータベースではなくデータソース |
| 日付・時刻の解釈は `UiSetting.timeZone` で固定する | 実行環境のローカル時刻に依存させると、サーバー（UTC）とブラウザ（JST）で描画がずれてハイドレーションが一致しない |
| ログイン開始はサーバー側で行う（`/auth/signin`） | クライアントJSのハイドレーション前でもログインできる必要があるため |
| Google Calendar 用のOAuthクライアントはDaySpan専用のGCPプロジェクトへ置き、公開ステータスを「本番」にする | 公開ステータスが「テスト」の間はリフレッシュトークンが7日で失効し、そのたびに設定画面からの再接続が要る（`GoogleReauthRequiredError`）。7日失効の条件は「テストであること」に紐づいており審査の完了は条件でないため、「本番」へ切り替えた時点で解消し、審査を待つ間も再接続は要らない。審査を通すのは「確認されていないアプリ」の警告と総計100ユーザーの上限を外すため。専用プロジェクトへ分けるのは、同意画面のスコープ一覧と審査がプロジェクト単位で、OAuthクライアントを分けても分離できないから。共有プロジェクトのまま審査へ出すと他アプリが登録したスコープまで審査対象に入る。手順は `docs/setup-checklist.md` §3、仕様は `docs/spec.md` §17 |
| 予定の文字色は背景の明るさから選ぶ | Google のパレットには淡い色があり、白固定では読めない。白で3.0を確保できる色だけ白のままにする |
| 日をまたぐ予定はドラッグ対象外 | 複数日表示でどちらの日を動かしているか決まらないため |
| 月表示はスクロールを契機に読み込み直さない | 再描画で開いていたダイアログが閉じ、操作を受け付けていないように見えるため |
| 繰り返しの曜日指定は `毎週(月・水・金)` の形で保存 | Notion の select プロパティ1つで表現する必要があるため |
| 日表示は前後1期間ぶんまで先に取得する | 左右スワイプが指に追従して隣の期間を見せるため。表示中の期間だけだと動かした先が空欄になる |
| 時間グリッドに並べる日は楽観的な anchor から決める | 前へ・次へ・スワイプで、取得の完了を待たずに隣の期間へ切り替わる必要があるため |
| 横スワイプは縦との勝敗が決まるまで日付を動かさない | 縦スクロール・予定のドラッグと同じ面の上の操作で、取り違えるとスクロールのつもりで日付が変わるため |
| 横スワイプの縦横の判定はブラウザより先に決める（6px） | `touch-action: pan-y` のもと、ブラウザは8px前後動いた時点で縦へ流すかどうかを自分で決める。それより後にこちらが判定すると、ブラウザは横だと決めた（＝縦に流さない）のにこちらは縦だと決めて何もしない指ができ、縦にも横にも動かないまま終わる（issue #329） |
| 横スワイプの追従はDOMへ直接書き、Reactのstateにしない | 動かす帯は3期間ぶんの幅と24時間ぶんの高さを持つ。指の動きごとにstateを更新すると、そのぶんの描き直しが毎フレーム挟まって指に遅れて動く。位置は `registerTrack` で登録された要素へ書き、動いている間だけ `will-change: transform` を立てる（常に立てるとその面を触っていない間も抱え続けるため） |
| 吸着中に指が置かれたら、その場で送りを確定させる | 吸着（220ms）の間の指を無視すると、日を送り続けたいときに1回おきに空振りする。行き先はもう決まっているので終点へ置いて確定させ、続きの操作をその位置から受ける |
| 日時の入力は日付と時刻の欄に分ける | `datetime-local` 1つだとスマートフォンで日付から順に選ばされ、時刻だけを直せないため |
| スワイプは期間ではなく1日ずつ動かす | 3日表示で3日ずつしか動けないと、今日を真ん中に置くような見方に切り替えられないため。前へ・次へは従来どおり期間ごと |
| カレンダーの空きを押したら簡易入力を出す | 日時は押した位置で決まっており、全項目を出すと直す欄より埋まっている欄が多くなるため |
| 長押しを受けるのは指・ペンのみ | マウスには押し続ける操作が無く、少し長めのクリックを追加と取り違えると、移動したいだけの操作が入力欄になるため |
| ダイアログの位置は className ではなく `position` で選ぶ | 位置・角丸・出方が組で決まる。上書きにすると基底の角丸や拡大の指定が残って打ち消し合うため |
| Service Worker は自前で書く（next-pwa / Serwist を使わない） | 保存してよいものをこちらで列挙できるほうが、認証付きのページや書き込みAPIを取り違えて保存する事故を防ぎやすい。新規依存も増やさずに済む |
| RSC（ソフトナビゲーション）の応答はキャッシュしない | 中身が `Next-Router-State-Tree` や先読みかどうかで変わり、別の状況で再生すると描画が壊れるため |
| オフライン判定は `navigator.onLine` ではなく `next/offline` の `useOffline` | `navigator.onLine` はOSのネットワーク接続しか見ず、WiFiには繋がっているが外へ出られない状態を `true` と答えるため |
| `sw.js` は `src/proxy.ts` の matcher から除外する | 未ログインだと `/login` へのリダイレクトがHTMLで返り、MIMEタイプ違いで Service Worker の更新が失敗するため |
| 毎年の日付リマインドはサーバー側で年ごとに展開し、IDへ日付を足す | 登録した年が表示範囲の外にあるため日付では絞り込めず、毎年の項目だけ別に取って展開する必要がある。IDが同じままだと、月ごとに保持するときに同じ項目として1件に潰れるため |
| 追加は種類を選ばせず入力画面を直接開く | 押す前に決めさせると、入力の途中で作りたいものが違うと気付いたときに、閉じて選び直すことになるため。予定・タスク・日付リマインドは1つのダイアログの中で切り替える |
| 入力ダイアログの枠は `ItemDialog` が持ち、中身だけ差し替える | Radixのダイアログを開いたままアンマウントすると `<body>` の `pointer-events: none` が残り、画面全体が操作を受け付けなくなることがあるため |
| 毎年の日付リマインドの編集は `sourceDate` を初期値にする | カレンダーに出ているのは展開した年の日付。そのまま保存すると毎年の起点そのものが動いてしまうため |
| ピンチ中の2本指スクロールは自前で動かす | 倍率を変えるには `touchmove` の既定動作を止めるしかなく、止めるとブラウザのスクロールも一緒に止まる。指を動かしても画面が固まって見えるため、掴んだ時刻を指の下へ留める計算の中で `scrollTop` も合わせる |
| 時間グリッドの位置は分で持ち、px はその都度求める | 1時間あたりの高さがピンチで変わるため。位置を px で持ち回ると、倍率を変えるたびに全ての値を作り直すことになる。重なり判定だけは既定の倍率で固定し、拡大しただけで予定の列の並びが変わらないようにする |
| タグ・種類の選択肢はNotion側を一次情報源にする | 選択肢と色はNotionのプロパティ定義そのもの。DaySpanへ写すとNotionで直接タグを付けた分と食い違う。DaySpanからできるのは追加・削除・改名・並び替えまでで、既存の選択肢の**色**だけはNotion APIが変更を受け付けない（`Cannot update color of select with id: ...`）。名前と並び順は選択肢のIDを添えて書き戻せば変わり、それが付いていた既存ページからも外れない（消して作り直すと外れる。shopping-listがv0.5.0〈改名〉・v0.6.0〈並び替え〉として同じ呼び出しの形で本番運用している）。並び順はそのまま入力画面のチップ・買い物のカテゴリのタブの並びになる。書き戻したあとは必ずNotionから取り直して返すので、仮に受け付けられなくても画面には変わらない一覧が戻るだけで、変えられたことにする表示にはならない |
| タグは文字入力ではなくチップから選ばせる | カンマ区切りの自由入力だと、同じつもりの名前が「仕事」「しごと」のように増えていくため。ただし入力画面から新しい名前も足せるようにする。閉じて設定画面へ回らせないため |
| タグの取得は予定・タスクの取得と分ける | 選択肢は月をまたいでも変わらない。カレンダーの取得に混ぜると、月を送るたびにNotionへの往復が増えるため |
| 場所の候補はNotionの場所DBを一次情報源にする | 予定・タスクと同じく、利用者が手で直せる場所に本体を置く。過去の予定から拾うと、直近の取得範囲に無い場所が候補から漏れる |
| AIに場所を尋ねるのは候補0件のときのボタン操作だけ | 呼ぶたびに利用枠を消費する。入力のたびに自動で呼ぶと、打っている途中の文字列で何度も呼ばれる |
| 場所は入力画面からその場で登録できる | 入力の途中で思いついた場所を使うために、設定画面やNotionへ回らせないため。タスクのタグを入力画面から足せるようにしているのと同じ理由 |
| AIの提案を場所DBへ登録するかは選ばせる | 一度きりの場所まで自動で登録すると、次からの候補が使わない場所で埋まるため |
| 場所の候補は入力欄の下へ押し出して出す | ダイアログの中に重ねると、スクロール領域の端で隠れてどこまで候補があるか分からなくなるため |
| 予定の場所はGoogleマップの検索で開き、座標があるときだけ座標で開く | 検索の形（`?api=1&query=`）にすると、スマートフォンではGoogleマップのアプリが入っていればアプリが開き、無ければブラウザの地図が開く。DaySpan側でアプリの有無を見分けなくて済む。座標を優先しないのは、場所欄が `名前 住所` の形で入るため文字列のほうが名前付きの結果に当たるから。座標を使うのは、地図から登録した「自宅」のように名前だけでは地点が定まらない場所（`placeCoordinates()` は名前が完全一致したときだけ返す）。場所欄が `http(s)://` で始まるときはそのURLを開く（会議のリンクを場所欄に入れている予定は、地図で検索しても意味が無い）。他のスキームは検索へ落とし、生の文字列を `href` に入れない。座標は画面がすでに読んでいる場所DB（`placeCatalog`）から引くため、Notionへの往復は増えない |
| 予定の場所のリンクはオフライン中は出さない | 地図もGoogleマップのアプリも通信が要る（docs/spec.md §21）。押せる見た目のまま残すと、押しても何も出ない操作になる |
| `html` / `body` に `overscroll-behavior: none` を指定する | スクロール可能領域が無い・端に達した状態でモバイルブラウザがページ全体をラバーバンドさせ、ヘッダー・フッターが指に追従してずれて見えるため。スクロール自体は妨げないため、内部スクロール領域や `settings` 配下のページスクロールとも両立する |
| 繰り返しの `UNTIL` は終日なら日付、時刻ありならUTCの日時にする | RFC 5545 では `DTSTART` の型と揃っている必要がある。ずれているとGoogleが繰り返しを作らないため。選んだ日の分まで繰り返したいので、時刻ありではその日の終わりを設定タイムゾーンからUTCへ直す |
| 繰り返しの間隔は「何週間ごと」のように入力欄のラベルへ単位を含める | 単位が頻度で変わる。欄の外に単位を並べると、スマートフォンで数字の欄が狭くなるため |
| 毎週の曜日は開始日の曜日を初期値にし、選び直したあとは開始日を変えても動かさない | 複数選択の欄が空のまま現れると何も繰り返さないように見える。一方で、指定した曜日が開始日の変更で黙って書き換わると、選んだつもりの曜日と実際が食い違うため |
| 削除は必ず確認を挟む | 押し間違えても画面上に戻す手立てが無い。繰り返し予定では1回分のつもりがシリーズ全体に及びうる。戻せるかどうかも保存先で違う（Googleの予定は戻せない、Notionはゴミ箱から戻せる）ため、実行前に示す |
| 削除は表示画面からも行える | 消すためだけに編集画面を開かせないため。編集画面にしか置かないと、消したい項目を開いて編集に切り替えるまで削除にたどり着けない |
| 繰り返しの「これ以降」は親のRRULEへUNTILを入れる | 画面に出ているのは `singleEvents` で展開した1回分で、そのIDを消してもその回しか消えない。境目は動かした先ではなく `originalStartTime` にする。動かした先を基準にすると、まだ消したくない回まで範囲に入るため |
| 予定日は期限とは別の枠として両方カレンダーに出す | 期限は締切、予定日はその辺りで片付けるつもりだという見込みで意味が違う。どちらか一方に寄せると、締切が見えないか、いつやるつもりかが見えないかのどちらかになる。同じタスクだと分かるよう形は変えず、予定日側だけ破線にする |
| 予定日と期限をまとめるかは、置く位置が重なるかで決める | 同じ場所に同じタイトルが2つ並ぶだけでは読める情報が増えないため。何を同じ場所と見るかは表示形式で違う。時間グリッドは日と時刻で位置が決まるので、日が同じでも時刻が違えば（片方が時刻なしで終日エリアに入る場合を含めて）分ける。月表示のマスは日までしか分かれないので、時刻が違っても同じ日ならまとめる。狭い画面では月表示の時刻そのものを出しておらず（`sm:inline`）、分けるとまったく同じ行が2つ重なって見える（issue #338） |
| カレンダー上のタスクは `タスクID:期限/予定日` で識別する | 1つのタスクが2枠に現れるため、IDだけではどちらを掴んでいるのか・どちらのキーなのかが決まらない。掴んだ枠の日付だけを動かすのにも要る |
| タスク一覧の分類・並び順は期限のままにする | 2つの日付で分類すると、同じタスクがどちらの区分に入るのか読めなくなるため。予定日は各行に添えるだけにする |
| カレンダー上の日付リマインドの印は菱形にする | ベルのアイコンは輪郭を読ませるために大きさが要り、9〜10pxの枠では項目名の幅をその分奪う。日付リマインドは項目名がそのまま何の項目かを示すため、形だけで見分けられればよく、タスクの縦棒と向きで分かれる菱形に置き換えた。この理由が当てはまるのは名前で種別が分かる項目までで、名前を読んでも種別が分からないゴミの日は例外にしている（下記） |
| 枠に添える補助ラベルは項目名と同じ文字列として流す | 別の要素にして縮まないようにすると、枠が狭いときに削られるのが項目名の側になり、年目や時刻だけが残って何の項目か読めなくなる。1つの文字列にすれば末尾から切れる |
| 空き時間の範囲選択はマウス・ペンだけで受ける | 指では同じ面の上で縦スクロールが起きるため、空きをなぞる操作と区別できない。押して簡易入力を開く従来の動作はどの入力機器でも残す |
| 範囲選択の列は押した日から動かさない | 縦に引く操作で横のぶれは避けられない。動かした先の列で作ると、斜めに引いただけで別の日の予定になる。日をまたぐ予定はこの操作では作らない |
| 範囲選択の枠は破線＋流れる縞にする | 保存前だと分かる必要がある。止まった枠だと、すでに置かれた予定と見分けが付かない。縞が流れ続けることで「いま押さえているところ」だと伝わる |
| ドラッグ直後のclickの打ち消しは全ての印を確かめる | どれか1つが立った時点で打ち切ると、確かめなかった側の「動かした」印が残り、次に普通に押したときのclickがそちらに食われる |
| 経過日数は過去の日付にだけ出し、登録した日から数える | 日付リマインドには誕生日・記念日のように過ぎた日付と、契約更新・有効期限のようにこれから来る日付が混ざっている。前者にしか「何日経ったか」は当てはまらない。数え始めを表示中の年にすると、毎年の項目で誕生日からの日数が毎年振り出しに戻るため `sourceDate` から数える。日付キーは `slice` ではなく `itemDateKey` で求める（時刻ありの項目はUTC表記だと日付が1日ずれる） |
| 活動記録は停止した時点で1回だけGoogleへ書き出す | 開始時に予定を作ると、終わりが決まるまで毎分ふやし続けることになり、外部APIへの往復が記録の長さぶん積み上がる（docs/spec.md §20「過剰なアクセスを発生させない」）。進行中はDaySpanのDBに1件だけ置き、画面の帯はそこから描く |
| 進行中の記録は予定を作れてから消す | 先に消すと、Googleの呼び出しが失敗したときに記録していた時間そのものが失われる。失敗しても記録は残り、もう一度停止を押せば保存できる |
| 記録の切り替えは前の記録を自動で終わらせる | いったん停止させてから次を押させると、その間ぶんの記録が空く。切り替えの前後で同じ時刻を使い、隙間を作らない |
| 記録中の帯は月表示に描かない | 月表示の週ごとの配置は、予定・タスクが変わったときだけ組み直している。毎分伸びる帯を混ぜると記録中はその計算が毎分やり直しになる。記録中かどうかは、どの表示形式でもメインナビの印が示す |
| 記録の開始・終了の時刻は、指定が無ければサーバーの時計で決める | 端末の時計がずれていると、記録した時間帯そのものがずれた予定になる。押し忘れ・止め忘れのために時刻の指定も受けるが、その値をそのまま信じるわけではなく、未来は受けない（入力欄が分単位のため、60秒ぶんだけ「いま」へ丸める）。開始より前の終了時刻も受けない。断る判定は画面だけでなくサービス層（`ActivityTimeRangeError`）にも置く |
| 時刻の指定は、押した時点で始める・止める経路と分けて置く | 記録の開始・停止は押す回数がいちばん多く、押すまでの手数がそのまま記録のずれになる。毎回時刻を確かめさせると、いちばん多い操作がいちばん遅くなる。開始は見出し横の切り替え、停止は記録中カードの中に置き、開いた人だけが日時の欄を見る。開始時刻の指定は始めたら閉じる（開いたままだと、次に押した項目まで気付かないうちに過去の時刻から始まる） |
| 時刻の欄を直していないときは、時刻を送らずサーバーの時計で決めさせる | 欄の初期値（「いま」）を作るのは端末の時計（`useNowIso()`）で、未来かどうかを決めるのはサーバーの時計。端末が1〜2分進んでいるだけで、欄を開いて何も直さずに押しただけの操作が「未来の時刻は指定できません」で断られる。しかも利用者からは原因が読めない。直したときだけ添えれば、開いただけの操作は従来と同じ経路（サーバーの時計）に乗る |
| 終了時刻の指定では、最短の長さまで伸びた後の時間帯を画面に出す | 開始が `10:00:30` のとき欄に入れられるいちばん近い値は `10:01` で、実際に保存されるのは `10:01:30` まで（`MIN_ACTIVITY_MINUTES`）。「開始+1分未満は断る」にすると、指定できるいちばん近い時刻がそのまま断られる。伸びた結果を先に見せるほうが、保存された予定と画面の数字も食い違わない |
| 下部ナビのクイックシートには時刻の指定を置かない | あのシートは見ている画面を離れずにその場で始めて止めるための最短経路で、時刻の欄を足すといちばん速い経路にいちばん遅い操作が入る。時刻を決めて記録したいときは、シートの「記録の画面へ」から入る |
| 活動記録はカレンダーの中ではなく専用画面に置き、メインナビの先頭にする | 記録を始める・終えるのは「いま」その瞬間の操作で、カレンダーのどの期間を見ているかとは関係が無い。カレンダー内のボタンにすると、押すまでにカレンダーを開いて目的のボタンを探す手順が挟まる |
| 記録中の印はメインナビの記録の項目とメニューボタンへ出す | 止め忘れたまま別の画面で作業していると、その間ずっと同じ項目を記録し続ける。どの画面からでも記録中だと分かる必要がある。そのためタスク・日付の画面でも `RunningActivity` を1行だけ読んで渡している。記録の項目がドロワーの中に入ったため（issue #463）、閉じたままでも分かるよう、その入口にあたるメニューボタンにも同じ印を出す |
| 活動記録の保存先は項目ごとではなくユーザーに1つ | 項目ごとに選べる形だと、設定画面に項目の数だけ同じ欄が並ぶわりにほとんどが既定のままで、保存先を変えるときは全ての項目を1つずつ直すことになる。`UiSetting.activityCalendarId` に持つ。進行中の記録（`RunningActivity.calendarId`）は始めた時点の保存先を写したままにし、記録中に設定を変えてもその記録の行き先は変わらない |
| 初期の項目は「入れたことがあるか」で判断する | 「1件も無ければ入れる」にすると、利用者が全部消しても開き直すたびに戻ってきて消せなくなる。`UiSetting.activityPresetsSeeded` を印にする |
| M3のカラーロールは `--color-<ロール>` まで `@theme` に出す | `--color-tertiary-container` だけを書いて `--color-tertiary` を出さないと、Tailwind v4 は `bg-tertiary` を未定義のユーティリティとして黙って捨てる。エラーにならず色が付かないだけなので、目盛りや細い線が消えていても気付きにくい |
| カレンダーの「表示」と「使用」を別のフラグで持つ | 見たいだけのカレンダー（共有された予定表・祝日）へ書き込ませないため。逆に、画面へ出さないカレンダーを保存先には使いたい場合もある。1つのフラグで兼ねると、書き込みたくないカレンダーを画面から消すしかなくなる |
| 書き込みの可否は `resolveGoogleAccountForCalendar()` で判定する | UIで隠すだけだと、DaySpanのAPIや将来のMCPから直接呼ばれた要求が素通りする。予定の作成・更新・移動・削除と活動記録の書き出しは全てこの関数を通るため、ここで断れば経路によらず同じ結果になる。なおGoogleへ直接つなぐ外部アプリはDaySpanの外にあり、この判定は効かない |
| 使用オフのカレンダーの予定は、編集・削除の入口ごと出さない | 押せるまま残すと、サーバーが断るまで直せるように見える。ドラッグも同じ理由で掴めなくする。複製だけは残す（別のカレンダーへ写せるため。初期値の保存先は既定へ落とす） |
| 活動記録の予定は保存先カレンダー単位で見分ける | 記録から作られた予定にGoogle側の目印を書いても、それ以前に記録した予定には印が無く対象から漏れる。設定で保存先に選ばれているカレンダーなら、過去の記録もGoogleで直接足した記録も同じ扱いになる。保存先「指定しない」が入る既定のカレンダーは、普通の予定も混ざるため含めない |
| 活動記録は時間グリッドでは左端のレーンへ置き、月表示には出さない | 記録は後から見返す事実で、これから動くために見る予定とは読む理由が違う。予定と同じ幅を取ると、睡眠のような長い記録が入った日は時間グリッドがほぼ記録の面になり、同じ時間帯の予定・移動がその半分へ押し込まれる（issue #327）。占めていた時間は帯の高さで残し、幅（16px）だけをレーンへ譲る。塗りを完全に外さないのは、長い記録で枠線しか見えず占有時間を面で追えなくなるため。月表示は1日に置ける件数が限られ、毎日入る記録がその枠を占めると何があるかが読めなくなる |
| レーンの活動記録は項目名を縦書きで出す | 記録用カレンダーは1つで色では分かれず、帯だけでは何の記録かが押すまで分からない。日本語は縦に組んでも読めるため、左の縦帯（3px）と枠線を除いた12pxに「睡眠」「仕事」が通る。レーンの中で記録どうしが重なる日は幅が半分以下になるため、そのときは帯だけにする（欠けた文字を出すより読めないことが分かる） |
| レーンへ移した記録はドラッグの対象外にする | 16pxの帯の中に上下のリサイズ領域が同居することになり、掴む先を狙い分けられない。動かすつもりが長さを変える操作になる。記録は過ぎた時間の事実で、掴んで動かす頻度も低い。時刻を直すときは押して表示画面から編集へ入る |
| 記録があるかどうかでレーンの有無を決める | 毎日空けると、記録を1件も付けていない日まで幅が減る。記録中の帯は開始日から今日までの列にかかるが、終わり（現在時刻）はクライアントでしか決まらないため、レーンを空けるかは日付の範囲だけで判定する。サーバーとブラウザで答えが違うと、最初の描画で予定の位置がずれる |
| 予定の表示画面からも紐づく移動を開ける | 移動は予定の背面に置くため（列を分けない判断の裏返しで）、時間が丸ごと重なる予定があるとその範囲では押せない。画面のどこに何が乗っているかに依らない道を1本持たせる。`linkedEventId` は既にあり、出していなかっただけ |
| ゴミの日は日付リマインドの読み取り経路を流用し、`ReminderItem.source` で分ける | プロパティ構成が日付リマインドDBの部分集合で、読み方も同じ。新しい `kind` を作ると、月表示・時間グリッド・並び順・チャンク保持の全てに同じ描画をもう1系統足すことになる。違うのは「編集させるかどうか」だけなので、1つの項目で分ける |
| ゴミの日の印はゴミ箱の線画にし、絵文字は使わない | 日付リマインドと同じ菱形だと、「普通ごみ」「資源ごみ」といった品目名を読むまでゴミの日だと分からない。週に2〜3件入るため読む手間がそのぶん積み上がる。菱形（6px）より4px太るが、品目名は短く飲める。絵文字（🗑️）は端末のフォント次第で色も形も変わり、印の並びで1つだけ多色になるうえ、ダークテーマでの見え方をこちらから直せない。lucideの線画ならアプリの他のアイコンと同じ描き方で、枠線と同じ tertiary に乗る（線画の印は `TaskStageMark` が先にあり、この枠に線画を混ぜること自体は初めてではない）。品目ごとには分けない（Notionへ渡るのは品目名の文字列だけ） |
| ゴミの日の収集時刻はNotion（myroom）が正で、DaySpanでは補わない | 収集時刻はmyroomの `data/garbage.json`（`collection_time`）に既にあり、Notionの日付プロパティへ時刻付きで書けばそのまま届く。DaySpan側にも「ゴミ出しの時刻」の設定を置くと、同じ意味の値が2か所に分かれ、画面に出ている時刻がどちらのものか読めなくなる。受け取り側の分岐も要らない。`hasTime` は日付の文字列に時刻が含まれるかだけで決まり（`normalizeDatePage()`）、終日エリアと時間グリッドの振り分けも `hasTime` しか見ていない（`source` では分岐していない）ため、時刻が入れば日付リマインドの時刻ありとまったく同じ経路でその時刻の位置に出る |
| ゴミ箱が削除ボタンと同じ図柄になることは許容する | アプリの削除ボタンは全て lucide の `Trash2`。ただし印は押せない枠の中の記号で、削除は詳細ダイアログ右上の押せるボタンと、置かれる面が違う。しかもゴミの日の詳細では削除ボタン自体を出さない（`source === "garbage"`）ため、同じ画面にゴミ箱が2つの意味で並ぶことはない。ゴミの日という主題ではゴミ箱が対象そのものを指しており、袋・カレンダー＋点のような代替はその直接さを失う |
| ゴミの日は日時も品目も同じページを1件に潰して返す | 粒度は「収集日 × 品目」で1ページ（`docs/spec.md` §9）なので、中身まで同じページが並ぶのは書き出し側（myroom）の作り直しの名残でしかない。myroomは次の同期で余分なほうを片付けるが、同期は1日1回のため、それまでの間はカレンダーに同じ品目が2つ並ぶ（issue #506）。DaySpanはこのDBを読むだけで消す手立てが無く、描く前に潰すしかない。潰すのは日時まで一致するものだけにする。同じ日で時刻だけが違う2件は、収集時刻を変えた後のページと変える前のページのどちらが今の値なのかをDaySpan側から決められず、片方を落とすと出す時刻を間違えて伝えることになる |
| 外部アプリが正のNotion DBは、UIだけでなくサービス層でも書き込みを断る | 入口を隠すだけだと、DaySpanのAPIや将来のMCPから直接呼ばれた要求が素通りする。`updateReminder` / `deleteReminder` は対象ページの親データソースが日付リマインドDBかを確かめてから実行する（往復は1回増えるが、読み取り側は増えない） |
| 移動の本体はDaySpanのDBに置く | Googleの予定はタイトル・場所・説明しか持てず、出発地と目的地を別々の欄に入れられない。場所欄に「自宅→渋谷」と書いても、そこから所要時間を計算し直せない。Notionに置かないのは、移動が予定から作られる派生データで件数も予定と同じだけ増え、予定を1件動かすたびにNotionへの往復が積み上がるため |
| 移動はGoogleへ書き出し、書き出したIDの予定を一覧から落とす | 他の端末の標準カレンダーからも移動が見える必要がある。ただし書き出した予定はGoogleからも取得されるため、そのまま描くと同じ移動が予定と移動の2つで並ぶ |
| 移動は先にDaySpanのDBへ保存してからGoogleへ書く | 逆にすると、DBへの保存で失敗したときにGoogle側だけ予定が残り、DaySpanからは触れない予定になる。Googleが失敗しても移動は残し、もう一度保存すれば書き出しをやり直せる |
| 既定の出発地は場所入力にし、AIへは住所を添えて渡す | 設定の欄が素のテキストだと `自宅` としか持てない。所要時間の見積もりは入力欄の文字列をそのままClaudeへ渡すため、住所の無い名前では地点が特定できず候補が常に0件になる（プロンプトが「特定できない場合は空の配列」を指示している）。予定の「場所」欄と同じ入力部品にすれば、候補から選ぶだけで `名前 住所` が入り、住所を持たない自宅を地図から登録し直す入口も同じ場所にできる。保存する値は文字列のままで、AIへ渡すときだけ場所DBの名前と突き合わせて住所を補う（画面に出る値・Googleへ書き出す値は変えない） |
| 設定 ▸ 移動 は保存後に `router.refresh()` を呼ばない | このページのサーバー側が既定の出発地の候補として場所DBを読む。`refresh()` を残すと、交通手段・帰りの移動・書き出し先を切り替えるたびにNotionの全件取得（`has_more` の間ページングする）が走る。表示は楽観更新で足りており、サーバーから取り直さないと崩れる箇所は無い |
| 所要時間はボタンを押したときだけAIに聞き、出どころを項目に持つ | 呼ぶたびに利用枠を消費する。入力のたびに呼ぶと、打っている途中の文字列で何度も呼ばれる（場所の「AIに聞く」と同じ）。出るのは目安で時刻表・道路状況は見ていないため、手で入れた値と区別できるようにしておく |
| 電車の所要時間はNAVITIMEから取り、窓口をtrainrouteに置く | 駅すぱあとのフリープラン（trainrouteが使っている）は経路探索の結果をURLでしか返さず、所要時間のJSONは上位プランでしか出ない。結果ページの読み取りは利用規約 第6条が「抽出」「フレーム内リンク」を明示的に禁じている。数値を返す取得元のうち、関西（阪急・大阪メトロ・JR西日本）を持ち、個人で契約でき、非公開のアプリから使えるのはNAVITIMEだけだった（ジョルダンのオープンAPIは「クローズドな目的にはご利用いただけません」、Google Routes APIは日本の公共交通が対象外、ODPTは関西の事業者データを持たない）。窓口をtrainrouteに置くのは、交通系のAPIキーを1か所へ集めるため。DaySpanはキーを持たず `TRAINROUTE_TOKEN` で内部APIを叩く |
| 経路検索は座標で行い、駅を選ばせない | NAVITIMEのトータルナビはドアtoドアで、最寄り駅までの徒歩を含んだ総所要時間を返す。出発地・目的地が駅である必要が無く、「自宅」に対して駅を選ばせる手順が要らない。座標は場所DBが既に持っており、無いときだけ住所からNominatimで引く。ただし座標がずれれば別の駅が選ばれるため、候補には実際に乗り降りした駅と徒歩の分数を必ず出す |
| 座標は画面から渡し、サーバーで引き直さない | 場所DBは入力ダイアログが既に持っている。サーバー側でNotionを引くと、ボタンを押すたびに場所DBの全件取得（`has_more` の間ページングする）が増える |
| 複数の経路が返っても、どれを使うかはDaySpanが決めない | 安い経路と速い経路のどちらを取るかはその日の事情で変わる。短い順に最大3件を候補として並べ、押されたものを採る（AIの見積もりを並べている候補リストをそのまま使う。同じ「電車」が複数並ぶため、行の `key` は交通手段では一意にならない） |
| 電車の所要時間はYahoo!乗換案内から取り込み、経路検索・AIは出さない | 実際のダイヤ上の列車が入る道があるのに、平均（NAVITIMEの経路検索）や目安（AI）を並べても、どちらを押すか決める材料が利用者に無い。Yahoo!乗換案内が経路を持たない車・徒歩・自転車は従来どおりAIで調べる。trainrouteの経路・設定 ▸ 移動の利用状況・既存の移動に残る `TRANSIT` は消さずに残す（画面から呼ばれなくなるだけで壊れず、消すと既存データの出どころと他の呼び出し元まで巻き込む） |
| Yahoo!乗換案内はDaySpanから開くだけで、結果ページを取りにいかない | Yahoo!は「情報や画面の二次利用については原則としてご遠慮いただいております」としており、プログラムによるデータ取得を認めていない。駅すぱあとの結果ページの読み取りを見送ったのと同じ判断（docs/spec.md §29「取得元の選定」）。読むのは、利用者が自分で選び自分でコピーした文字列だけにする。共有の短縮URLに経路の発着時刻は入っておらず、URLから読むこともできない |
| Yahoo!乗換案内は座標（`flatlon` / `tlatlon`）で開く | 渡すと最寄り駅までの徒歩を含んだドアtoドアで探索され、「自宅」に対して駅を選ばせる手順が要らない（NAVITIMEのトータルナビへ座標を渡しているのと同じ理由）。座標は場所DBから引く。場所欄には `名前 住所` の形が入っており、その文字列のままでは地点が引けないため、座標が無いときは住所を検索語にする。リンクは素の `<a>` で置く（iOSがUniversal Linkでアプリを開けるようにするため。スクリプトから開くと、アプリが入っていてもブラウザへ落ちることがある） |
| Yahoo!乗換案内は場所DBの「最寄り駅」を先に見る | 座標だけを渡すドアtoドアの探索では、Yahoo!が選ぶ乗り降りの駅が地点のわずかなずれで変わる。同じ場所から出るのに、開くたびに別の駅から乗る経路が出ると、比べる基準が無い。駅名が入っていればいつ開いても同じ駅から探せる。駅名を渡すときは座標を添えない（座標があるとYahoo!はそちらで探索し、駅名は表示名にしかならない）。最寄り駅は場所DBの任意プロパティで、無い場所は従来どおり座標→住所の順に落ちる。Notion側の対応付けは名前が当たったときだけ行う（座標と同じ `rich_text` のため、型だけで空き欄へ割り当てると「メモ」のような欄へ書き込む） |
| Yahoo!乗換案内へ `名前 住所` の文字列を丸ごと渡さない | 場所欄には候補から選ぶと `名前 住所` が入るが、Yahoo!はこの文字列から地点を引けず、**日時指定ごと捨てて検索画面を返す**（`cmd=4011`。カンマ区切りの `名前, 住所` は `cmd=4001`）。指定した日時で検索されない原因がこれで、URLの組み立て（`y`/`m`/`d`/`hh`/`m1`/`m2`/`type`）自体は正しかった（issue #458）。住所だけ・駅名だけなら引ける。場所DBとの照合は完全一致のあとに**名前で始まっているか**まで見る（住所を後から直すと完全一致は外れるが、名前は重複を断っているので1件に決まる）。それでも当たらない値は、先頭以外に現れる**最後の**都道府県名で名前と住所に分けて住所のほうを渡す（`src/lib/place-text.ts`）。最初ではなく最後を採るのは、名前の側に都道府県を含む値（`カフェ大阪府庁前 大阪府大阪市…`）で最初を採ると住所側が `名前 住所` の塊のまま残り、分ける前と同じ失敗に戻るため。都道府県が見つからない値（施設名だけ・英語の住所）はYahoo!がそのまま引けるので触らない。分割・照合・地点の決定は `resolveYahooPlace()` に閉じ、画面はそれを呼ぶだけにする（画面の中の非公開関数だと、確かめるときに写しを書くことになる） |
| Yahoo!乗換案内で探す基準はチップで選ばせ、既定は到着時刻にする | 予定から移動を足すと出発時刻には仮の値（開始の30分前）が必ず入る。時刻が入っているかどうかで基準を決めると、その仮の値で探すことになり、予定に間に合わない経路まで候補に並ぶ。一方で、前の予定が終わってすぐ出るような移動では出発時刻の側が決まっており、到着時刻の欄を空にしないと出発基準で探せなかった（その間は保存もできない）。どちらで探すのかが画面から読める必要もあるため、選択そのものを画面へ出す。押した側と実際に開くURLが食い違わないよう、チップの選択状態も `resolveYahooTransitBasis()` で決め、選んでもその側にならない（＝時刻が空の）チップは押せなくする |
| 取り込んだ経路の日付は入力欄のまま動かさない（例外は予定に紐づかない新規の移動） | Yahoo!側で「今から」検索した結果をそのまま入れると、来週の予定の移動が今日へ飛ぶ。カレンダー上で移動だけ別の日へ動き、ダイアログを閉じたあとでは気付けない。入れるのは時刻だけにし、検索されていた日が移動の日と違うことは取り込みの報せに添える。到着が出発より前になる経路（終電）は日をまたいだものとして到着を翌日にする。**ただし右下の「＋」から作った、まだ保存していない移動（予定に紐づかない・編集中でもない）だけは、検索した日をそのまま採用する**（issue #490）。動いて困る予定の日がそもそも無いため |
| 取り込みで必須なのは発着時刻だけにする | 日付・所要時間・乗換回数・路線名・駅名は、読めればメモの要約と報せに使う。Yahoo!側の文面が変わったときに、1項目の欠けで時刻まで失わないため（trainrouteの応答の読み方と同じ扱い）。発着時刻が読めなければ何も書き換えず、貼り付け欄へ落とす |
| 出発地・目的地を何も入れていなくてもYahoo!乗換案内を開けるようにする | 地点がどちらか欠けると地点未指定のトップページ（日時パラメータなし）を開く。日時や場所を決める前から検索できるようにするため（issue #490）。片方だけ入れた状態も同じ扱い（Yahoo!自身も片方欠けでは地点を引けず日時ごとトップページへ落とすため、上の「`名前 住所`を丸ごと渡さない」と同種の挙動） |
| 取り込んだ乗車駅・降車駅は、出発地・目的地欄へ貼り付けるたびに上書きする | 新規の移動では設定の既定の出発地（例:「自宅…」）が最初から入っており、「欄が空のときだけ埋める」だとほぼ発火しなかった。貼り付けは実際に選んだ経路という強い意思表示であり、既定値やそれまでの値より優先してよい（issue #498）。駅名に付く `(都道府県)` の同名駅区別の注記は欄に入れる前に落とす（`yahooStationName()`）。残すと `splitNameAndAddress()` が丸括弧内の都道府県名を住所と誤認し、次にYahoo!リンクを開くときの地点解決が崩れる |
| 取り込んだメモは要約せず、案内文・注意書きだけを除いた生テキストをそのまま使う | 発着駅・時刻・路線名から作り直した要約は、要約する過程で所要時間・運賃・距離・号車/番線・運賃内訳・共有URL・乗換駅名などが欄から漏れる（issue #498）。`[Yahoo!乗換案内]` より前の部分（`noteText`）をそのまま使えば、貼り付けた内容が丸ごと残る。区切りに `[Yahoo!乗換案内]` マーカーだけを使うのは issue #491 の対応のまま |
| Yahoo!乗換案内からコピーした経路の広告文言は `[Yahoo!乗換案内]` マーカーだけで区切る | 行頭が`※`の行も区切りに使うと、運賃欄の注記（`※IC優先運賃を表示しています`等）のように経路詳細の途中に現れる`※`行にも反応し、乗換回数・駅名・路線名が丸ごと消える（issue #491）。広告文言（アプリDL案内・定期代の注記）は必ず`[Yahoo!乗換案内]`から始まるため、そこだけで区切れば十分 |
| 経路検索が取れなかったら、断らずAIの見積もりへ落とす | 未接続・未デプロイ・利用枠切れ（月500回のハードリミット）・応答なしは、いずれも「この区間は分からなかった」であって移動を作れない理由ではない。落ちる形にしておけば、trainroute側が未対応のままDaySpanを先にマージしても何も壊れない |
| 経路検索の結果も「平均」だと断る | 返るのは乗換の待ち時間を平均で見込んだ標準的な経路で、時刻表上の特定の列車ではない（電車時刻表データはNAVITIMEのオプション契約が必要で、APIマーケット経由では使えない）。出発時刻まで入るぶん確定したダイヤだと受け取られやすい |
| 所要時間の出どころは3値で持つ（`TravelEstimateSource`） | 経路検索の結果をAIの見積もりと同じ扱いにすると、あとから「この数字はどこから来たのか」を読めなくなる。`estimated`（真偽値）は「手入力ではない」の意味に寄せ、画面に「（目安）」を出す条件として残す |
| 移動はドラッグの対象外にし、予定の重なり計算にも混ぜない | 時刻は紐づく予定から決まっており、掴んで動かすと予定とのつながりが黙って崩れる。列を分けないのは、移動が予定に付随するもので、横に並べると予定の幅がそのぶん狭くなるため |
| 移動を足せるのは時刻のある予定だけ | 終日予定には「何時までに着けばよいか」が無く、出発時刻を逆算する起点が決まらない |
| 移動の帯は塗り・枠線とも通常の予定と同じにし、矢印だけ残す | 背景色をGoogle側の書き出し先カレンダーの色にしたのは、「同じカレンダーに保存されているのに色が違う」状態を解くため（issue #492）。その代わりに足した左端の縦線・斜め縞は、同じ「移動」カレンダーへ**予定として**保存したものには付かず、同じ場所へ同じつもりで入れたものが2通りの見た目で並んでいた。入力経路（移動タブ / 予定タブ＋保存先カレンダー）の違いを画面で区別する必要は無いため、縞と縦線をやめて予定と同じ塗り・枠線（`eventColors()`）へ戻す（issue #502）。矢印（`TravelMark`）だけは残すが、色は固定の専用色（travel）ではなく`eventColors()`が選んだ文字色に乗せる。青緑のままだと濃い色のカレンダーで矢印だけが背景に沈み、移動だと分かる手掛かりが何も残らないため。月表示のチップは塗りを持たず背景が surface なので、専用色の矢印のまま残す |
| 経路検索APIの残り回数はtrainrouteが数え、DaySpanは表示だけ | NAVITIMEを呼ぶのはDaySpanではなくtrainroute（交通系の外部APIの窓口）。DaySpanが自分の呼び出しを数えると、他の呼び出し元（AIDE等）が消費した分が抜ける。正は提供元（RapidAPI）が応答に付ける残数ヘッダー（`x-ratelimit-requests-limit` / `-remaining` / `-reset`）で、`GET /api/internal/transit-quota` から受け取る |
| 残り回数を見るためだけの問い合わせはしない | 残数は経路検索を呼んだついでに返る。見るたびに問い合わせる形にすると、残りを確かめる操作そのものが枠を消費する。trainrouteは最後に見た値を1行だけ持ち、DaySpanはそれを出す（画面の数字は前回の検索時点の値なので、最終更新を添える） |
| 枠のリセット日は暦の月末として扱わない | RapidAPIの枠が戻るのは契約日からの請求サイクル。「今月」で数えると実際の枠とずれる。`-reset`（残り秒数）から出た日時をそのまま出す |
| 経路検索の利用状況は、取れなければ区画ごと出さない | trainrouteと連携していない間はNAVITIMEを一度も呼んでおらず、出す数字が存在しない。「取得できませんでした」を設定画面に常駐させても、利用者にできることは無い |
| 残り回数は見積もりの応答に添えて返す | 入力画面のために別の往復を1つ増やさないため。引くのは経路検索の**あと**にする。押した直後に、いま使った1回が引かれた値へ変わらないと、減ったことがその場で見えない。**電車をYahoo!乗換案内へ移して以降、入力画面には出なくなった**（見積もりのAPIを呼ばないため）。残るのは設定 ▸ 移動 の表示だけで、そこには「DaySpanはこの枠を使わない」と添える。数字だけを出すと、移動を作るたびに減っていくものだと読める |
| 枠切れのときだけ、AIの目安になった理由を出す | 前回まで実データで出ていた電車の所要時間が黙って目安に戻ると、値が変わった理由が画面のどこにも出ない。一方、枠が残っていて取れなかったとき（座標が引けない・trainrouteが応答しない）に同じ文を出すと理由を偽ることになるため、従来の断り書きのままにする。**電車をYahoo!乗換案内へ移して以降は、枠切れでも電車の所要時間の入れ方は変わらない**ため、設定 ▸ 移動 の断りも「他のアプリが調べられなくなる」へ改めた |
| アプリアイコンの色は `src/lib/app-icon-glyph.tsx` に集約する | 背景色は favicon・apple-icon・PWAの192/512・`manifest.ts` の `theme_color` の5箇所で使う。各ファイルに直接書くと、一部だけ変えたときに端末によって違う色のアイコンが出る |
| 狭い画面のカレンダーヘッダーは月だけ出す | 1日・3日表示で「8月16日 – 18日」まで並べると、スマートフォンでは操作の並んだヘッダーに収まらず `truncate` で切れ、いま見ている期間そのものが読めなくなる。日と曜日は列ヘッダーに出ているため、狭いときは月（月をまたぐときは「8月 – 9月」）に落とす。表示中の幅で文字列を選ぶとサーバーとブラウザで結果が変わるため、長短の両方を描いてCSSで隠す |
| ウィジェットの認証は専用トークンにする | ウィジェットの更新は利用者が操作していない時点でiOSが走らせる。Scriptableはブラウザのログインセッションを持てず、Supabaseのセッションでは認証できない。できるのは活動記録の読み取りだけに絞る |
| ウィジェット用トークンは `Authorization` ヘッダーで受ける | クエリに載せるとApacheのアクセスログへそのまま残り、ログを見られるだけで他人の記録を読めるようになる |
| ウィジェット用トークンはハッシュで引き、暗号文で再表示する | 暗号文はIVが毎回変わるため値では引けない。一方でハッシュだけを持つと台本をコピーできるのが発行直後の1回だけになり、機種変更や台本の作り直しのたびに他の端末のウィジェットが黙って動かなくなる |
| `/api/widget/` と `/api/internal/` は proxy の中でSupabaseを通さず素通しする | 認証がトークンで完結しており、更新のたびにSupabase Authへ往復させる理由が無い。Supabaseへ到達できない間、トークンだけで判定できる要求まで503になるのも避ける。matcherから外さないのは、外すと詐称されたユーザーIDヘッダーが後段へ届くため |
| ウィジェットの今日の集計は保存先カレンダーを指定しているときだけ出す | 未指定の記録は予定作成の既定のカレンダーへ入り、そこには普通の予定も混ざるため、どれが記録なのか区別できない。カレンダー画面での描き分けと同じ理由 |
| 台本はURLとトークンを埋め込んだ状態で配る | 利用者に値を貼り込ませると、打ち間違いに気付ける場所が実機のウィジェット（何も出ない）しかない |
| ウィジェットはブラウザではなくホーム画面のDaySpanを開く | iOSは `https://` のリンクを必ずSafariで開き、ホーム画面のWebアプリへ渡さない。SafariとWebアプリはストレージが別扱いのため、Safariへ落ちるとログインし直しになることがある。Webアプリを開けるのは `webapp://<ホスト>` スキーム（iOS 16.4以降）だけ。パスは無視され最初の画面（`start_url`）から開くが、その最初の画面が記録のため同じ所へ着く。ただし追加済みの端末で `start_url` が新しい値へ入れ替わる時期はiOS側の都合で決まる（`/manifest.webmanifest` は Service Worker が stale-while-revalidate で扱う）。カレンダーが開く端末には追加し直してもらう。ホーム画面に追加していない端末では開けないので、台本の先頭の `OPEN_IN` でブラウザへ切り替えられるようにする。`http` のアドレスで作った台本は `webapp://` を組み立てない（httpはWebアプリとして追加できない） |
| ウィジェットの経過時間はiOSに数えさせる | iOSはウィジェットを台本が要求した間隔どおりには更新しない。サーバーが求めた分数を文字として置くと次に台本が動くまでその値で止まり、開いて見るたびに古い時間が出る。開始時刻を `WidgetDate` へ渡してタイマー表示にすれば、台本を動かさずに進み続ける。引き換えに、別の端末で止めたときは次の更新までカウントが進み続ける |
| ウィジェットの今日の集計は予定の配列だけを3分持ち回す | ホーム画面とロック画面の両方に置くと枠の数だけ更新が走り、そのぶんGoogleへの往復が増える。iOSは同じアプリのウィジェットをまとめて更新するため、短時間持つだけでその回が1回に収まる。合計を計算した後の数字を持たないのは、記録中のぶんが現在時刻で伸び続け、合計だけが止まって見えるため。記録を止めて予定を作った直後は捨てる |
| ライブアクティビティはDaySpanからは出せない | ホーム画面のWebアプリ（PWA）にはActivityKitのAPIが無く、ScriptableにもLive Activityを扱うクラスが無い。出すには第三者のブリッジサービス（専用アプリの導入・有料・記録名の外部送信）か自前のネイティブアプリが要る。ロック画面のウィジェット＋タイマー表示で、経過時間が常に進む点だけは代替している |
| ウィジェットを押したときの飛び先は、台本だけでなくウィジェット編集画面でも案内する | 台本の `widget.url` を設定しても、iOS側の「ウィジェットを編集」の `When Interacting` が既定の `Run Script` のままだと押してもScriptableが開く。この画面はDaySpanから設定できず、`URL` 欄は利用者が手で入力する。打ち間違いに気付ける場所が実機のウィジェット（何も出ない）しかないのは台本と同じなので、項目と値を設定画面へ写してコピーさせる。ラベルはScriptableの画面に出ているとおりの英語のまま並べる（訳語にすると端末の画面と見比べられない） |
| Scriptableのアイコンを押したら見本を出さずDaySpanを開く | 押す理由はウィジェットと同じ「いまの記録を見たい・止めたい」で、ウィジェットの見本を挟むと目的の画面まで1手増える。開くだけなら記録の取得も要らない |
| アイコンの背景はテーマ色の紫、図柄は白にする | 背景をアプリのテーマ色（`--md-primary` = `#6750a4`）に合わせると、ログイン画面のボタンやPWAの `theme_color` と同じ色になる。この紫と白のコントラストは6.4:1あり、32pxのfaviconでも枠が背景に沈まない。中の縦棒は塗らずに背景色で抜き、色数を増やさずに枠と棒の境目を出す。背景を淡い色に変えるときは、白のままだとコントラストが3.0を割るため図柄側も暗い色へ戻す |
| サーバー間参照は `/api/internal/*` を新設して分ける | 既存のユーザー向けAPIへAPIキー分岐を足すと、1つの経路が「セッションでも共有シークレットでも通る」形になり、返す内容を変えるたびに両方の呼び出し元を考えることになる。分けておけば、画面用の応答はいつでも画面の都合で変えられる |
| `INTERNAL_API_KEY` 未設定は素通りではなく503にする | 未設定を「認証しない」と解釈すると、設定漏れがそのまま無認証の公開に化ける。使えないことを明示して断るほうが、気付ける場所が早い |
| サーバー間APIの基準日はDaySpan側が決める | 日付の解釈は `UiSetting.timeZone` に閉じている。呼び出し元に基準日を作らせると、サーバーのローカル時刻（UTC）で組み立てた日付が渡り、日本時間の 00:00〜09:00 が前日になる事故が呼び出し元の数だけ起きる |
| 期限切れタスクはカレンダーの取得範囲を広げずNotionへ別に取りにいく | `loadCalendarData()` は1つの範囲をGoogle・Notion・移動の3経路へ渡す。期限切れのために範囲を30日広げると、タスク1種類のために表示中のカレンダー全部の予定を毎回31日ぶん取ることになる。Notionへの往復1回を惜しむより取得量を要求ぶんに留めるほうが軽い |
| 連携が未設定かどうかを応答に添える | Google未接続・NotionのDB未設定は失敗ではなく空で返る。`errors` にも出ないため、呼び出し元では「その日は何も無い」と見分けが付かない |
| サーバー間APIの対象ユーザーは `ALLOWED_GOOGLE_EMAILS` が1件のときだけ引く | APIキーとユーザーの対応表を持たずに済ませるための割り切り。黙って先頭を選ぶと、利用者を増やした瞬間に別人の予定を返す。決まらないなら断る |
| カレンダーの前回の状態はCookieに1時間だけ持つ | `/calendar` はサーバーコンポーネントで、表示形式と日付から取得範囲を組み立ててから描く。localStorageに持つと描き終えてから読むことになり、今日の月を取ったあとに前回の期間をもう一度取り直す（外部APIへの往復が倍になり、画面も一度ちらつく）。期間を1時間に切るのは「基本は今日を月表示」を既定に保つため。期限はCookieの `max-age` で表し、サーバー側に時刻の判定を持たない |
| URLの `view` / `date` は前回の状態より常に優先する | 再読み込み・ブックマーク・共有されたURLの意味が変わらないため。書かれていない項目だけを記憶で埋める |
| カレンダーの前回の状態は起動をまたがない | 記憶（Cookie・1時間）は「少し前に見ていた続き」を戻すためのもので、閉じて開き直したときまで効くと、起動のたびに先月・来月や1日表示のまま出る。起動は `sessionStorage` に印が無いこと＝ブラウジングコンテキストが新しく作られたこととして判定する。再読み込みとオフライン時のハードナビゲーション（issue #321）では同じコンテキストが続くため印が残り、起動と誤判定しない。破棄は書き込み（`rememberCalendarView`）と同じクライアント側で完結させ、サーバー側の読み取りは変えない。判定は書き込み側にも置く。`CalendarShell` はマウント時にサーバーが描いた状態を書き戻すため、`/calendar` から起動した場合は破棄と書き込みのどちらが先かが決まらず（`loading.tsx` のSuspense境界の内側で水和の順序が保証されない）、書き込みが後になると前回の状態が同じ値で書き直され、1時間の期限まで延びて記憶が終わらなくなる |
| カレンダーの前回の状態はログインもまたがない | ログインを求められる＝セッションが途切れた体験で、次に見るのは今日から始めたい。しかしブラウジングコンテキスト（タブ）自体は変わらないため、起動判定（`sessionStorage`・上記の決定）には掛からず、記憶が生き残ってしまう。`/auth/callback` がセッション確立に成功した直後、リダイレクトを返す直前でCookie（`dayspan_calendar_view`）を明示的に消す（issue #486） |
| タスクのタグは分類の軸として期限と切り替える | 期限の区分の中をさらにタグで小分けすると、1区分に1〜2件しか入らないときに見出しばかりが並ぶ。タグを絞り込みだけに使うと、まとまりごとに追いたいという要求（issue #286）に届かない |
| 複数タグのタスクはどの区分にも出す | 先頭のタグの所だけに置くと、並び順がNotion側のタグの定義順に依存し、探したタグの下に無いことが起きる。重複するぶん件数の合計は総数と一致しないが、探して見つからないほうが困る。見出しに出ているタグは行から外し、兼ねている他のタグだけ残す |
| 完了は分類の軸によらず末尾に1つ置き、既定で畳む | 履歴として残すため件数が増え続ける（docs/spec.md §12）。開いたままだと下へスクロールするほど完了済みが占める。タグごとに分けないのは、履歴を探すきっかけがタグではなく時系列のため |
| タスク画面の見え方はCookieではなく端末（localStorage）に持つ | 分類の軸・並び順・完了の開閉は、サーバー側で取得範囲を決めるのに使わない。カレンダーの表示形式・日付（Cookie）と違い、描いたあとに読んで足りる |
| 優先度はバッジではなく行左端の色帯で示す | 「高」の文字が日付・予定日・タグと同じ列に混ざり、縦に並べたときどれが急ぐのか読み取れない。色だけに意味を持たせないよう、読み上げ用の文字を添える |
| 日付リマインドの一覧は次に来る日で並べる | 一覧を開く理由は「次に何が来るか」を見ること。毎年の項目にとって日付プロパティの年は登録した年でしかなく、その年でまとめると誕生日のような起点の古い項目が先頭に沈む（issue #288）。見出しを月だけにして年が変わる位置にだけ年を添えるのは、行の中に出る年（起点）と別のことを指すため |
| 毎年の日付リマインドの行に経過日数を出さない | 同じ行の「あと5日」と役割が重なる。「24,102日経過」は行の中でいちばん長い文字列で、項目名を押し出す。正確な日数は表示画面に残す |
| 過ぎた一回限りの日付リマインドは末尾に畳む | 次に来る日を持たないため、時系列の先頭に置くと直近の項目がその下に沈む。タスク画面の完了と同じ扱い。ただし毎年プロパティ未設定のDBでは全項目が一回限りになり、誕生日・記念日までここへ入るため、その構成では既定で開く。開閉を端末へ残さないのは、この画面に覚えさせる見え方が他に無く、そのために保存の仕組みを1本増やす理由が無いため |
| スマホ下部の「カレンダー」は日付だけ今日へ動かす | 表示形式まで1日表示へ固定すると、月表示で使っていても今日を見るたびに表示形式を選び直すことになる（issue #175 は「今日ボタンと同じ動作」を求めていた）。カレンダー画面から押されたときはURLではなく画面内の「今日」（`goToday`）を呼ぶ。URLだけ書き換えても、すでに描かれている月表示はその場に留まるため |
| 地図は自前で描き、地図ライブラリを入れない | ここで要るのは「中心の1点を選ぶ」ことだけで、経路・レイヤ・マーカー群といったライブラリの主機能を使わない。タイルを敷いてドラッグとピンチを受けるところまでで足り、Service Workerを自前で書いているのと同じく依存を増やさずに済む |
| 地図のピンは画面の中央に固定し、動かすのは地図の側 | 押した位置にピンを落とす方式だと、押している指がその地点を隠す。拡大縮小のたびにピンと地図がずれるのも避けられない。拡大縮小も中心を軸にすれば、選んでいる地点は操作の間ずっと動かない |
| 地図の位置は緯度経度で持ち、pxはその都度求める | ピンチで倍率が変わるため。pxで持ち回ると倍率を変えるたびに保持している値を全部作り直すことになる（時間グリッドの位置を分で持つのと同じ理由）。タイルは整数のズームしか無いので、途中の倍率はいちばん近いズームのタイルをCSSで拡大縮小して見せる |
| 住所の取得（Nominatim）はサーバー側から呼ぶ | 利用規約が呼び出し元を名乗るUser-Agentを求めるが、ブラウザのfetchでは差し替えられない。1秒に1回を超える連続呼び出しも禁じられているため、地図が止まってから1回だけ呼ぶ |
| 地図から取れた施設名は、名前を触ったあとは上書きしない | 打った文字列は利用者が決めた名前。地図を少し動かすたびに「南改札」のような近くの施設名へ差し替わると、入れたはずの名前が消える |
| 住所が取れない地点は緯度経度を住所として登録する | 地点自体は決まっており、登録を止める理由が無い。数字のままでもGoogle Calendar側で地図は引ける |
| 場所DBの「座標」は名前が当たったときだけ対応付ける | 住所と同じ `rich_text` のため、型だけ見て空いているプロパティへ割り当てると、既存DBの「メモ」のような無関係な欄へ座標を書き込む。既存DBには足りないので、設定画面から追加できるようにする |
| 追加の種類の切り替えに1行の説明を添え、名前を「日付リマインド」に揃える | タスクと日付リマインドの違いは完了状態を持つかどうかの一点（`docs/spec.md` §9）だが、選ぶ時点では画面のどこにも出ていない。「リマインド」という短い名前は「後で思い出させてくれるもの」と読め、やることの置き場として選ばれやすい。やって終わらせるものを入れても完了を付ける場所が無く、毎年やる作業でも今年やったかが残らない（issue #292） |
| 日付リマインドを新しく作れるのは専用一覧（`/reminders`）だけにする | 誕生日・契約更新日は一度入れたら数年触らないのに対し、カレンダーとタスク一覧の「＋」は毎日押される。そこへ完了状態を持たない種類を並べていること自体が取り違えの入口だった。外すのは新規登録の入口だけで、カレンダー上での表示・編集・削除は従来どおり（issue #295） |
| `/reminders` の「＋」からはタスクも作れるままにする | 迷ったときはタスクのほうが安全で、完了を付ける場所があり、繰り返しなら次回分も作られる。日付リマインドは完了を持てないため、取り違えたときに失うものが大きい |
| アプリを開いたときの最初の画面は記録にする | 記録を始める・終えるのは「いま」その瞬間の操作で、起動してから探して押すのでは間に合わない（メインナビの先頭に置いているのと同じ理由）。設定で選ばせないのは、PWAの `start_url` が静的でユーザーごとに出し分けられず、設定に持たせてもホーム画面から起動した場合に効かないため。値と `next` / `callbackUrl` の検証は `src/lib/home-path.ts` に集約する。ルート・`start_url`・ログイン後の戻り先の4経路がずれると、ウィジェットの着地点の前提まで崩れるため。設定画面の戻り先も起点に揃えて記録にする（PCでは設定の入口がカレンダーの歯車だけのため来た場所とは別の画面へ戻ることになるが、戻り先はアプリの起点で揃える判断） |
| 予定に紐づけたタスクの日時は、選んだ行き先（Notionの「期限」か「予定日」）へ書く | カレンダーの取得・描画・ドラッグを既存の日付の経路のまま使える。3つ目の枠を足すと、1つのタスクが期限・予定日と合わせて3枠に現れ、同じ場所に同じ名前が並ぶだけになる。行き先を選ばせるのは、「この会議で提出する」のように締切そのものが予定で決まる場合があるため。選べないと、そのぶんを手で書き写すことになる（issue #342）。既定は予定日にして、選べるようになる前と同じ操作で同じ結果になるようにする |
| 紐づけは行き先ごとに1件にする | 期限と予定日は別の予定に従いうる（会議で提出する資料を、前日の準備の予定に合わせて作る）。一方、同じ行き先の2件目は入る先が1つしか無く、持てても置けない。行き先を変えるのは「解除してから紐づけ直す」にする。付け替えを受けると、移った先にすでにある紐づけをどう扱うかがもう1通り増える |
| 月表示でまとめた枠には、期限と予定日の両方の紐づけの印を並べる | 月表示のマスは日までしか分かれず、同じ日の期限と予定日は期限の1枠にまとまる（issue #338）。まとまった枠は両方の日付を表しているため、片方の印を落とすと、その紐づけの段階もずれも月表示のどこにも出なくなる。印は2つで7px程度で、項目名は末尾から切れる |
| APIの応答の形を変えたら `public/sw.js` の `VERSION` を上げる | Service Workerは `/api/calendar` `/api/tasks` の応答を保存しており、古い形の応答が新しいJSへ渡る。項目が増えるだけなら `?.` で凌げるが、形が変わる変更（`link` → `links`）では読む場所で落ちる。世代を上げても古い世代が制御している間は前の応答が返るため、読む側でも欠けた項目を受けられるようにしておく（`withTaskLinks`） |
| 紐づけ中の日付は入力画面に出さず、書き換えられたらその行き先の紐づけを外す | 直せるようにすると、入れた値が次に予定が動いた時点で黙って書き戻される。隠すだけではDaySpanのAPIや将来のMCPから直接呼ばれた要求が素通りするため、サーバー側でも突き合わせる。外すのは書き換えられた行き先の紐づけだけにする。期限を直したからといって、予定日の紐づけまで外す理由は無い |
| 予定とタスクを結ぶ線はDaySpanのDBに置く | Googleの予定にもNotionのタスクにも「相手を指す欄」が無く、足せば利用者のDBの構成を変えることになる。移動（`TravelPlan`）と同じ考え方で、線だけを`TaskEventLink`が持つ |
| 紐づけに予定名の写しを持つ | 一次情報源はGoogleだが、タスク画面や取得範囲の外では予定本体が手元に無く、何に紐づいているのかを示せない。予定が手元にあるときは最新の値へ差し替えて渡す |
| 「実施中」は開始時刻、「終了まで」は終了時刻に置く | どちらも予定の最中を指すが、置く時刻で分かれる（issue #72 のコメントの「2と3は同じかも？」への答え）。段階から日時をずらさない（「30分前」を足さない）のは、段階が「いつ」ではなく「予定のどの位置か」を表しており、何分前が適切かは予定の中身で違うため |
| DaySpanの外での予定の変更には自動で追随しない | 取得のたびにNotionへ書き戻すことになり、読み取りの途中で外部APIへの書き込みが積み上がる（docs/spec.md §20）。ずれは取得時に判定して画面に出し、押されたときだけ書き戻す。予定が取得範囲に無いときは判定しない（範囲外か消えたのかを区別できず、消えたことにすると範囲を送るたびに警告が出る） |
| 予定を消しても日付（期限・予定日）はタスクに残す | 消すと、紐づけが外れただけで「いつまでにやるつもりだったか」まで失われる。要らなければその日付を未設定にすればよく、そちらは戻せない操作ではない |
| 紐づけ先のタスクは一覧をNotionへ別に取りにいく | 期限も予定日も無いタスクはカレンダーに出ていないが、それこそが「いつやるか決まっていない」＝紐づけたいタスク。カレンダーが持っているタスクからは選べない |
| 紐づけの書き込みはNotionが先、DaySpanのDBが後 | 逆にすると、Notionへの書き込みが失敗したときに「紐づいているのに日付が入っていない」行が残る。先にNotionへ入れておけば、DBの保存で失敗しても残るのは普通の期限・予定日だけで、もう一度紐づければやり直せる |
| 予定の更新では終日と時刻ありの使わない側を `null` で送る | GoogleのPATCHは `start` / `end` の中身まで既存の値へ重ねる。`date`（終日）と `dateTime`（時刻あり）は排他で、終日の予定を時刻ありへ変えると `date` が残ったまま両方が載り、`Invalid start time.` で拒まれる。作成（POST）には重なる既存の値が無いため送らない |
| カレンダーに置く項目の角丸は `--radius-item` 1つで持つ | 種類ごとに書くと同じ面に複数の丸みが並び、項目の種類の違いなのか意味の違いなのかが読めない（issue #316 の時点で 12px・8px・4px が混在していた）。値は4px。0pxだと時間の罫線と輪郭が同じ直線になり、連続した予定のどこまでが1件かが読めなくなる。M3のシェイプスケール（`--radius-xs` 〜 `--radius-xl`）はダイアログ・ボタン側にそのまま残す |
| オフライン中のメインナビの移動はハードナビゲーションにする | ナビの移動はソフトナビゲーション（RSC要求）で、その応答は保存していない（中身が `Next-Router-State-Tree` や先読みかどうかで変わり、別の状況で再生すると描画が壊れるため）。オフラインでは `experimental.useOffline` がその要求を再接続まで保留するので、`start_url` の記録から押しても骨組みが出たまま止まり、カレンダーへたどり着けない（issue #321）。ハードナビゲーションなら、起動・再読み込みで既にオフラインでも開けている Service Worker の経路をそのまま使える。保存済みが無いときは切り替えず、従来どおり保留する（ブラウザのオフラインエラー画面へ落とすとアプリごと失う） |
| 開いている画面が自分のページを Service Worker へ保存させる | ブラウザがHTMLを取りにいくのは起動と再読み込みのときだけ。ナビから移動して使っている限り、その画面はいつまでも保存されず、オフラインで開けない。取り直すのは保存済みが10分より古いときだけにする。`/calendar` の描画はGoogle・Notionを叩くため、開くたびに温め直すと外部APIへの往復が画面ぶんとウォームアップぶんで倍になる（docs/spec.md §20）。温めるのは自分の画面だけで、開いてもいない画面のぶんは取らない |
| 保存済みのクエリ違いで代用してよいのはページだけ | `/api/calendar?months=…` の応答を別の月の要求へ返すと、`splitByMonth()` が要求した月に該当しない項目をすべて捨てるため、その月が「取得できて、予定が1件も無かった」ように見える。取れなかったことにするほうが正しい。ページは表示形式・日付が違っていても手掛かりになるため従来どおり代用する |
| 重なる印はラベルだけを縦へ送り、引き出し線を折る | タスク・日付リマインドの印は高さを持たず、近い時刻のものは同じ位置に重なって上の1件しか読めない（issue #331）。予定と同じ列分割にできないのは、印の中身が項目名そのもので削るところが無いため（2件で「資料提…」、3件で「資」まで縮む）。縦棒は時刻を指しているので動かさず、ラベルを送ったぶんだけ線を折って本来の時刻へ戻す。押し出す量は表示中の高さから決める。予定の列分割を既定の倍率で固定しているのとは逆の判断で、あちらは倍率を変えるたびに左右の並び順が入れ替わると読み直しになるのに対し、印は拡大すれば押し出しが解けるだけで並び順は変わらない |
| オフラインかの判定は `useOffline()` に `navigator.onLine === false` を足す | `useOffline()` は false から始まり、ブラウザの `offline` イベントか Next.js 自身の要求（ナビゲーション・先読み・Server Action）の失敗で初めて true になる。オフラインのままPWAを起動した直後は、ページもJSも Service Worker が返して何も失敗せず、状態の変化も無いので `offline` イベントも起きない。クライアントの素の `fetch()`（`/api/calendar`）の失敗もNext.jsは見ていない。`navigator.onLine` は「WiFiには繋がっているが外へ出られない」を true と答えるため true 側は信用できないが、false は信用できる。描画には使わない（サーバーとブラウザで値が食い違い、ハイドレーションが一致しない）ため、押されたとき・取得に失敗したときにだけ読む |
| 記録は下部ナビの中央に置き、はみ出した円にする | 押す回数がいちばん多いのに、他と同じ形で端に並べると「いま始める・止める」たびに探して押すことになる。記録は「いま」その瞬間の操作で、探す手間がそのまま記録の始まり・終わりのずれになる（docs/spec.md §27）。5項目にするのは、記録がちょうど中央に来る並びだから（5枠目の買い物リストはissue #434で実装した） |
| 記録の円は上へだけはみ出させ、ラベルの高さは他と揃える | 円ごと持ち上げると「記録」の文字だけが上にずれる。1項目だけ基準線が違うと、どれがいま選ばれているかを読む前に段差が目に入る。円は他の項目と同じ高さのアイコン枠の中へ `absolute` で置く |
| 設定は下部ナビから外し、ヘッダー左上のメニューへ移す | 設定は毎日押すものではないのに、毎日押す項目と同じ大きさで1枠を占めていた。ドロワーは既存のRadixダイアログに `position="left"` を足して使い、依存は増やさない（背景を押す・Escで閉じる・フォーカスの閉じ込めがそのまま手に入る） |
| メニューボタンはどの画面幅でも出し、広い画面の移動もドロワーに集約する | 以前はカレンダー・タスク・記録・日付・買い物の5項目と勤務・場所・設定をヘッダーへ横一列に並べていた。カレンダーでは同じ帯に前へ・次へ・年月・今日・表示形式・再取得も乗り、押しどころが13個になって、いま見ている期間がその列の中に埋もれる（issue #463）。ヘッダーにはその画面の操作だけを残す。ドロワーの中身は画面幅で変えない（同じアプリの中で探す位置が幅によって入れ替わらないようにするため）。狭い画面では下部ナビと道が2本になるが、下部ナビは押す回数のいちばん多い5項目への最短経路としてそのまま残す。引き換えに広い画面では画面の移動が1タップ増える |
| ドロワーのバージョンは更新履歴への入口にする | 数字だけを出しても、その版で何が変わったかは結局設定から探すことになる。押せる行にしておけば、バージョンを見た流れのまま読める |
| クイックシートの項目は長押しされたときに取りにいく | 下部ナビはカレンダー・タスク・日付の画面にも出る。各画面のサーバー側で項目を読むと、開くたびにDBの読み取りが1つずつ増える。記録中かどうかも同時に取り直す（別の端末で開始・停止していることがあり、ナビが持つ印はサーバー描画の時点の値のため） |
| スマートフォンのカレンダーの「今日へ飛ぶ」アイコンは出さない | ヘッダー左上はメニューになった。狭い画面で今日へ戻る操作は下部ナビの「カレンダー」が同じことをする（issue #175）。PCは下部ナビが無いため、アイコンをそのまま残す |
| 通知はすべてサーバーから送る | iOSのホーム画面Webアプリには「この時刻に鳴らして」と端末へ予約する手段が無い（Notification Triggers非対応）。時刻が来たことに気付けるのはサーバーだけで、アプリを開いていない間に鳴らすには外から送るしかない |
| 通知の文面は先に作ってDBへ置く | 送る直前に予定・タスクを取りにいくと、通知1件ごとにGoogleとNotionへの往復が増える（docs/spec.md §20）。取得は30分に1回にまとめ、送信（毎分）はDaySpanのDBしか見ない |
| 1時間以上遅れた通知は送らない | デプロイやサーバーの再起動をまたいだ通知は、届いても意味が変わっている。1時間前の「まもなく会議」が起動と同時にまとめて出ると、いま何が起きているのか読めなくなる |
| 通知の許可は端末ごと、内容はアカウントごと | 許可はブラウザが端末ごとに持つもので、iPhoneとPCで別々になる。一方「何分前に知らせるか」を端末ごとに変える理由は無く、端末の数だけ同じ設定を直すことになる |
| 時刻の無い期限はまとめて1通にする | 1件ずつ送ると、同じ時刻に同じ見た目の通知が期限の数だけ並ぶ。件数と先頭の数件が分かれば、開くかどうかは決められる |
| 終日の予定は通知しない | 「10分前」に当たる時刻が無い。日付が変わった瞬間に知らせても、その日の行動には結び付かない |
| バッジは期限だけで数える | タスク画面の分類の軸が期限のままのため（`classifyTasks()`）。予定日を混ぜると、見出しの「期限切れ」「今日」の合計とアイコンの数字が食い違う |
| バッジを更新するのは通知が届いたときとアプリを開いたとき | iOSは通知を出さないプッシュ（バッジだけの更新）が続くと購読そのものを取り消す。件数のためだけに送る経路は作れない。開いている間の取り直しは10分に1回までに絞る |
| 記録中の通知は開始時に1通だけ出す | 経過時間を伸ばすには送り直すしかなく、そのたびに通知が積み上がる。進み続ける経過時間はiPhoneウィジェットが受け持つ（docs/spec.md §28）。止めたときに消せるのは操作した端末のぶんだけで、消すためだけにもう1通送ると「記録が終わりました」という通知が増える |
| 通知の送信は `web_push: 8030` の形で送る | iOS 18.4以降はService Workerを起こさずそのまま表示する（Declarative Web Push）。それより前のiOSと他のブラウザは `push` イベントで受け取るため、`public/sw.js` が同じJSONの `notification` を読む。1つの形で両方を賄えるので、送信側は端末を見分けない |
| 通知のタイマーは `src/instrumentation.ts` に置く | 送る時刻はリクエストの外で来る。PM2はDaySpanを1プロセス（fork・instances: 1）で動かすため、この中で数えていれば二重に送られない。VPSのcronにしないのは、別リポジトリの変更が要り、デプロイしても設定を入れるまで通知が動かないため。手で走らせる入口（`/api/internal/notifications/dispatch`）は別に置く |
| VAPID鍵は1行のbase64urlで持ち、起動時に対を検証する | PEMを環境変数へ入れると改行が `\n` の文字列になり、復元し損ねると「鍵が途中で切れている」形の失敗になる（signalyが踏んでいる）。公開鍵と秘密鍵は別々の変数にあり、片方だけ入れ替わっていても値としては読めてしまう。その状態では購読は作れるのに配信だけが落ち続けるため、秘密鍵から公開鍵を計算し直して突き合わせる（JWKで x・y を渡す形ではNodeが d との対応を確かめない） |
| 通知を足しても `public/sw.js` の `VERSION` は上げない | 上げると `activate` で古い世代のキャッシュがまとめて消え、オフラインで開けていた画面が一度失われる。世代を上げるのは保存している応答の形が変わったときで、`push` の受け取りを足すことは応答の形を変えない |
| 入力画面のボタンは保存・削除の2つだけを全幅で縦に置く | 「やめる」は右上の ✕・画面外のタップ・Esc とまったく同じことをする4つ目の出口で、押す先が増えるだけで戻れる道は増えない。保存を下端で幅いっぱいにすると、スマートフォンで片手のまま届く。削除に塗りを持たせず error 色の文字だけにするのは、塗りの有無で差を付けると目が向く先が保存側になり、押し間違いの向きもそちらへ寄るため。確認ダイアログの「やめる」は残す（そこでは出口ではなく、2つある答えのうち安全なほうそのもの）。表示画面の「閉じる」も残す（下端のボタンが1つだけで、上下で意味の違うボタンを取り違える余地が無い） |
| 入力欄の ✕（クリア）は文字を打つ欄にだけ出す | 日付・時刻の欄は消しても保存できない必須項目で、✕ を置くと押した先が必ず入力エラーになる（端末のピッカー側にも消す手立てがある）。チップ・選択も「空にする」ではなく「選び直す」操作しかない。出すのはタイトル・場所・出発地・目的地・説明・メモで、予定・タスク・日付リマインドは1つのダイアログの中で切り替えるため、タイトル欄だけ ✕ が出たり消えたりしないよう3種類に揃える。共通の入力部品（`Input` / `Textarea`）の `onClear` を渡した欄だけに出し、全ての入力欄へ自動で付けない（設定画面・タグ編集まで一度に変わる）。値の有無で ✕ を出し入れしても**枠の中の構造は変えない**。同じ位置の子要素が label から div へ入れ替わるとReactが input を作り直し、1文字目を打った瞬間にフォーカスと変換中の文字が飛ぶ |
| 起動画面はアイコンと同じ紫の面にする | OSが出す起動画面は1色（`background_color`）しか持てない。アプリ側をテーマに追従させると、ダークの端末で「白い起動画面 → 暗いアプリ」と一度光る。アイコンの背景と同じ紫に揃えると、iOSが `background_color` とアイコンから作る起動画面ではアイコンの角丸の器が背景に溶け、白い図柄だけが残ってアプリ側の起動画面と同じ絵になる。境目でアイコンが動かなければ、空白があった時間はそのまま起動画面が続いていたように見える |
| 起動画面はレイアウトのオーバーレイにし、`loading.tsx` にしない | ルート単位の `loading.tsx` にすると、下部ナビで記録の画面へ移るたびに全面のアイコンが挟まる。記録は押す回数がいちばん多い（docs/spec.md §27）。オーバーレイなら、`<html>` の `data-app-ready` を立てて消したあと同じドキュメントの中で二度と出ない。`<body>` の先頭に置くのは、ページの描画を待つあいだにシェルだけが先に流れるため、そこに入れておくと最初のチャンクで描かれるから |
| 起動画面の消し方はCSSに持たせ、時間切れでも消す | 消す合図（`data-app-ready`）はハイドレーション後に立つ。JSが動かなかったときにJSで消す作りだと、サーバーが描いた中身が起動画面に覆われたまま操作できなくなる。中身はすでに下にあるため、8秒で消してよい |
| ルートにも `loading.tsx` を置くが、中身はブランド面にしない | 置く理由は表示のためだけではない。この境界が無いと動的なページはレンダーが終わるまでHTMLが1バイトも流れず、レイアウトに置いた起動画面の描画までサーバーの応答を丸ごと待つことになる。一方で `loading.tsx` は配下の画面すべてのフォールバックになるため、ここへ起動画面を置くと日付リマインド・設定へ移るたびに全面のアイコンが挟まる。中身は面とプログレスバーだけにし、下部ナビから開く画面と設定にはそれぞれ自分の形の骨組みを持たせて、ルートへ落ちないようにする |
| 下部ナビから開く画面の骨組みには、ナビの帯まで描く | ナビは画面をまたいで残り続ける枠で、骨組みから落とすと押したナビそのものが一瞬消えて戻る。`BottomNavSkeleton` を4画面で共有する |
| 起動画面はオフライン中のナビの移動でも出る | オフラインではナビの移動がハードナビゲーションに切り替わり（issue #321）、ドキュメントごと作り直されるため。`data-app-ready` はドキュメントに紐づく |
| `background_color` の変更はすぐには端末へ届かない | `/manifest.webmanifest` は Service Worker が stale-while-revalidate で扱い、追加済みのWebアプリで新しい値が使われる時期はiOS側の都合で決まる（`start_url` と同じ）。OSの起動画面が紫になるのは追加し直したあとで、それまで白いのは不具合ではない |
| 勤務場所・出張・年休はNotionに置き、DaySpanのDBにもGoogleにも持たない | 勤怠の提出や経費の精算のために後から見返し、月単位で数える記録で、利用者が手で直せる場所に本体が要る（タスク・日付リマインド・場所と同じ）。1日1件（年365件）で予定に付随して増えるものではないため、Notionへの往復が積み上がらない（移動をNotionに置かなかった理由の逆）。Googleへ書き出さないのは、毎日1件の終日予定がカレンダーの終日エリアを毎日占めるため |
| 通常の勤務と出張を同じNotion DBの同じ形のページにする | 違うのは日付が単日か期間かと「出張」チェックの有無だけ。種類ごとにDBを分けると、月の一覧を出すのに2つのDBへ問い合わせることになる |
| 勤務場所は1日1件、出張は期間で1件 | 午前・午後で分けられる形にすると押す回数が倍になり、毎日の1タップが崩れる。一方で事前申請・事後登録は出張1回につき1回で、日ごとに持つと3日間の出張で同じチェックが3つ並び、どれを押せば済んだことになるのか決まらない。重なりを許さないのは、同じ日に「在宅」と「出張」が両方立つと月の集計でどちらに数えるか決まらないため。Notionに一意制約が無いので保存のたびにDaySpan側で確かめる |
| 出張・事前申請・事後登録のプロパティは名前が当たったときだけ対応付ける | 3つとも checkbox で、型だけを見て空いている欄へ順に割り当てると事前申請と事後登録が入れ替わりうる。入れ替わったことは画面からは分からず、申請したはずのものが未対応として残り続ける（場所DBの「座標」と同じ理由） |
| 事後登録は終了日を過ぎてから未対応に数える | 出張の前から未対応に混ざると、まだできないものが件数に含まれ続け、いま手を打つべき件数として読めなくなる |
| 勤務は専用画面に置き、記録画面には混ぜない | 勤務場所は毎日入れるものだが、記録のように「いま」その瞬間を押さえる操作ではなく、一日の終わりや翌朝にまとめて入れられる。記録画面は「いま何かを始める・終える」ための画面で、そこへ別の目的の欄を足すと、押しに来た操作が探しにくくなる |
| 下部ナビの4枠目は勤務にし、日付リマインドはドロワーへ移す | 勤務は出張・年休の申請漏れを気にする画面で、日付リマインドより開く頻度が高い（issue #508）。勤務画面は他の4画面と同じ「ヘッダーのメニューボタン＋下部ナビ」の骨格（`SettingsShell` の戻るボタン付きの形から変更）にし、対称に日付リマインド画面は勤務が元々使っていた「戻るボタン付き」の骨格（`AppMenuButton`/`BottomNav`をやめる）へ移した。ドロワーには「画面」区画の1行として「勤務」が引き続き出るため、開いたときだけ取得する未対応件数のバッジ（`/api/work/alerts`）はそのまま残す。下部ナビの「勤務」自体にはバッジを出さない（5画面すべてに常時表示され、ドロワーと同じ「開いたときだけ取得する」形を適用できないため）。ドロワーの「そのほか」区画（日付・場所・設定）の行はそもそも`active`（選択状態）を持たない設計で、その画面を開いている間はメニューボタン自体が無くドロワーへ到達できないため |
| 出張扱いの勤務場所は `NotionConnection.workTripPlaces` に持つ | 行けば必ず出張になる勤務先があり、選ぶたびにスイッチを入れ直すと毎日の1タップが崩れる。UiSettingではなくNotion連携の側に置くのは、勤務場所の選択肢が勤務記録DBのselectのプロパティ定義そのもので、DBを選び直せば選択肢ごと入れ替わるため。UiSettingへ置くと、DBを差し替えたあとも一覧に無い場所名が残り、どの画面からも外せなくなる。選択肢を消したときも同じ理由で出張扱いから落とす |
| 出張扱いの指定は設定 ▸ タグの勤務場所に添える | 勤務場所の選択肢の一覧がそこにあり、場所を足した流れのまま指定できる。専用の設定画面を作ると、同じ一覧が2か所に出る |
| 出張のスイッチは手で触るまで場所の既定へ追従する | 触ったあとも場所を選び直すたびに書き換えると、「今日は出張にしない」と外した状態が黙って戻る。逆に一度も触っていない状態で追従しないと、場所を選ぶだけで出張になるという既定そのものが効かない（毎週の曜日を選び直したあと開始日を変えても動かさないのと同じ扱い）。すでに出張として保存されている記録は、場所とは無関係にそう決められたものなので追従させない |
| 場所から出張になった単日の記録はチップから取り消せる | 1押しで登録したものを1押しで戻せないと、消すためだけに日の行から入り直すことになる。手で作った出張（行き先が場所名と違う・複数日にまたがる）は対象外にする。チップで上書きすると行き先や期間が消えるため |
| 年休は勤務場所の選択肢ではなく専用のプロパティ（`年休` select）にする | 選択肢に「年休」を足すだけなら実装は要らないが、その振る舞いが選択肢の名前に縛られる。Notionで「有休」に直した瞬間に申請のチェックが消え、直した本人にも原因が読めない。DaySpanが名前で決めてよいのはプロパティの対応付けまでで、利用者が自由に足せる選択肢の名前に振る舞いを持たせない |
| 年休の申請済は出張の `事前申請` checkbox を共用する | 指しているものが同じ（事前に出す申請が済んだか）。手続きの checkbox を増やすと、名前が一致したときだけ対応付ける項目がそのぶん増え、取り違えの余地も増える。とりわけ事前申請と事後登録は名前も型も似ており、入れ替わったことが画面からは分からない。年休と出張は同じ日に立たないため、1つの欄で足りる。年休が事後登録を持たないのは、休んだことを後から届け出る手続きが無いため。列を足してよいのは、名前で明確に分かれて他の欄と取り違えようがないものだけ（`会社休業日`・下記） |
| 年休の区分は checkbox 2つではなく select 1つで持つ | 「年休か」「半休か」を分けると、半休なのに全休が立つ組み合わせを作れる。select なら未設定＝年休ではない。半休は 0.5 日として数える（区分名に「半」を含むかで決める）。勤怠の提出で見るのは月の日数そのもので、半休を1日として数えると使えない |
| 出張と年休の同時指定はサーバー側でも断る | 月の集計でどちらに数えるかが決まらない。画面では3択にして選べないようにしているが、隠すだけだとDaySpanのAPIや将来のMCPから直接呼ばれた要求が素通りする（勤務場所の1日1件と同じ扱い） |
| 勤務場所はカレンダーの日付の見出しに置く | 1日1件で時刻も長さも持たない「その日そのものの属性」。予定・タスク・移動と同じ面へ置くと、毎日必ず1件あるぶんだけ月表示の段と終日エリアが埋まり、Googleへ書き出さないと決めた理由がそのままDaySpanの中で起きる。日付の見出し（月表示は数字の右、時間グリッドは数字の下）は日数によらず高さが決まっているため、置いても他の面を押し出さない |
| 勤務場所のチップは押せる印にしない | 日付の見出しを押したときの行き先は「その日の1日表示へ移動」で既に決まっている。そこへ別の行き先を重ねると、日を開くつもりの操作が入力画面になる。登録・修正は `/work` に閉じる |
| 勤務場所の名前を出すか色だけにするかを画面幅で切り替えない | 列の幅は表示形式と画面幅の掛け算で決まる。`sm:` のようなブレークポイントで切ると、スマートフォンの1日表示（列は全幅）でも名前が消える。残った幅をそのまま使う（色だけに意味を持たせないよう、読み上げ用の文字を添える） |
| 勤務場所の名前は文字の途中で切らない | pxで切ると、スマートフォンの月表示（数字の右に残るのは約21px）で2文字目が数pxだけ顔を出した形で切れ、色も形も壊れて見える（issue #406）。1文字ずつ折り返させて1行ぶんの高さで隠すと、切れ目が必ず文字の境目に来て、入る文字だけが丸ごと残る。1文字ぶんも入らない幅では名前ごと出さず色だけにする。その判定を画面幅でやるとPCの月表示のような幅のある列でも名前が消えるため、チップに残っている幅そのもの（`@container` と `@max-[12px]:hidden`）で決める。月の変わり目の日は数字が `9/1` のように広がるため、その日だけ数字の左右の余白を詰めて残り幅を確保している |
| 狭い列では、出張の印より行き先の名前を先に残す | 出張だけ印（ブリーフケース）を名前より優先していたため、スマートフォンの月表示ではカバンの絵しか出ず、行き先が読めなかった（issue #433）。チップに入るのは行き先（「大阪」）で、その日について読みたいのはそちら。印と名前が両方入る幅（余白8px＋印10px＋間隔3px＋文字10px＝31px以上）でだけ印を添え、それより狭ければ印を落とす。印を丸ごと廃止しないのは、31px以上では両方入るため捨てる理由が無いから。引き換えに狭い列では出張と通常の勤務を分けるものが色だけになるが、種別は読み上げ（`sr-only`）と勤務の画面・1日表示に残る |
| 狭い列では勤務場所チップの文字を8pxへ落とす | 10pxのままだと、幅390pxの端末の月表示（チップに残るのは約21px）で1文字しか入らず、「大」だけでは行き先も勤務場所も読めない（issue #433）。8px・左右の余白2pxにすると2文字（16px）＋余白4pxで収まる。31px以上の列（PCの月表示・1日/3日/週表示）は10pxのままにする。全幅で8pxにしないのは、幅のある列でも名前が今より小さくなるため。8pxにするかを幅で決めて出張かどうかで分けないのは、同じ月表示の中で出張の日だけ文字の大きさが違うと、幅で決まっているはずの見え方が種別で変わって見えるから（狭い列では通常の勤務・年休・会社休業日も8pxになるが、そこは10pxでも1文字しか入らない幅）。高さ（`leading-[15px]`）は変えないので日付の見出しの高さは動かない |
| 出張はブリーフケースの線画で分ける | 出張の勤務場所には行き先（「大阪」）が入るため、名前だけでは通常の勤務と区別が付かない。ゴミの日をゴミ箱の線画で分けているのと同じ扱い |
| 勤務記録のNotionへの問い合わせには下限日を置く | 勤務記録は1日1件で増え続け、下限が無いと過去の全件をページングする（5年で約1,800件＝18往復）。カレンダーは月を送るたびにこれを通る。Notionの日付フィルタは範囲の開始日しか見ないため、下限を範囲の開始日そのものにすると範囲より前に始まって続いている出張が落ちる。現実的な出張の長さを超える幅（92日）でさかのぼる |
| 未対応の件数はドロワーを開いたときに取りにいく | 各画面のサーバー側で数えると、勤務を開かない日もNotionへの往復が画面の数だけ増える（記録の長押しシートと同じ扱い）。取れなければ数字を出さないだけで、メニューそのものは開ける |
| 勤務・場所・設定の入口はドロワーだけにする | 3つともヘッダーにアイコンで置いていたのは、PCがドロワーを持たなかったため。どの画面幅でも開くようになった以上、この帯に残す理由が無い（issue #463）。同じ経路に並べておけば、探す場所が1つ増えない |
| 勤務の画面の上部には未対応の手続きだけを出す | あの2つの区画を開く理由は「まだ済ませていないものを片付けること」。済んだ出張・年休をその月のあいだ並べたままにすると、いま手を打つべきものがその中に埋もれる。区画に出す記録の判定は `workTodos()` をそのまま使い、日付の規則を画面側に持たない。済んだ記録は日別の一覧に残っており、行を押せば入力ダイアログから事前申請・事後登録を外せる。事前申請・事後登録のプロパティが無いDBでは中身が常に空になるため、区画ごと出さない（issue #412） |
| 行のチェックボックスは事前申請・事後登録のどちらも常に出し、押せないものはdisabledで示す | 以前は出張が終わるまで事後登録をチップごと外していたが、それだと事前申請だけが行に出ている出張に事後登録も要ることが読めなかった（issue #509）。押せるかどうか（終了日 < 今日）と、区画にその記録を出すかどうかの判定は別で、他の理由（主に事前申請未）で行が出ている出張であれば事後登録は常に見える。入力ダイアログの事後登録チェックボックスも同じ式でdisabledにし、理由（「出張終了後に登録できます」）を添える |
| 勤務の一覧では日曜と祝日を同じ赤（`error`）にする | 一覧を見ながら決めているのは曜日ではなく「その日が働く日かどうか」。月曜の祝日が平日と同じ灰色だと、入れ忘れなのかそもそも働いていない日なのかが読めない。土曜は休みでも出社しうるため青（`travel`）のまま分けておく |
| 勤務画面の月切替は、出張・年休の区画と「この月の勤務場所」のあいだに置く | 出張・年休の区画は月の外の記録も含めて手続きが残っているものだけを出しており、月に限定された情報ではない。月切替をヘッダー直下（いちばん上）に置くと、その下に続くこれらの区画にも効くように見えてしまう。月ごとの内容の直前に置き、「この月の勤務場所」の見出しと一体化させる（issue #510） |
| 勤務の日別一覧の未対応は、一括り表記ではなくチェックボックスで事前申請・事後登録を区別し、出張の開始日の行にだけ出す | 「未対応」「未申請」の一括り表記では、開いて確かめるまでどちらの手続きが残っているか読めなかった。`workTodos()`（終了日を過ぎるまで事後登録を含めない関数）をそのまま使ってチェックボックスを並べ、その場で完了にできるようにする。押せる範囲は上部の出張・年休の区画（`shownWorkTodos()`）と違い、日別の一覧では常に未完了のものだけなので、既存の判定をそのまま流用でき出張中に事後登録を誤って完了にできる余地も生まれない。出張は期間の全ての日に同じ記録が並ぶため（`coversDate()`）、チェックボックスを全ての日の行に出すと同じ手続きに対するチェックボックスが日数ぶん重複する。開始日（`record.startDate`）の行にだけ出す（issue #510、計画レビューG1の指摘） |
| 登録の無い土日祝は「休み」として表示だけ自動的に扱い、書き込まず月間集計にも含めない。会社休業日のタブへは寄せない | 基本的に土日祝は休みで、登録の無い日が平日と同じ「未登録」で並ぶと、入れ忘れなのか休みなのかが読めない。対象を土日だけでなく祝日にも広げるのは、日曜・祝日を赤で示す色分け（上記）と理由が同じ（月曜の祝日が平日と同じ表記だと読めない）ため。かといって毎週2日ぶん自動でNotionへ書き込むと、利用者が指示していない書き込みが積み上がる（過剰なアクセスを避ける方針・`docs/spec.md` §20 と同じ考え方）。表示だけの既定にすれば書き込みは増えず、集計（勤務場所ごとの日数）も実際に登録した記録だけを正にできる。明示的に記録を残したいときは新しい種類を増やさず、既存の会社休業日の登録をそのまま使う（ユーザー確認）が、日を開いたときの初期値は土日祝でも「勤務」のタブのままにする。会社休業日は「会社が決めた休み」で期間で1件のもの（下記）で、土日を1日ずつ会社休業日として登録すると、月の集計（`Tally()`）が本来のお盆・年末年始の休業日数と混ざって読めなくなるため（issue #510、計画レビューG1の指摘） |
| 祝日は取りに行かず `src/lib/japanese-holidays.ts` で計算する | 祝日は法律で日付が決まっており、Google Calendarの祝日カレンダーから取ると月を送るたびに往復が増える（`docs/spec.md` §20）。利用者がその祝日カレンダーを購読しているとも限らない。サーバーとブラウザで答えが一致する必要もある（片方だけ色が付くとハイドレーションが一致しない）。ライブラリを足さないのは依存を増やさないため（Service Workerを自前で書いているのと同じ）。対象は2007〜2099年（春分・秋分の近似式の範囲と、振替休日が現行規定になった年） |
| 祝日は色だけでなく名前も一覧の行へ出す | 赤いだけでは何の日か分からず、色以外の手掛かりが無い（優先度を色帯＋読み上げ用の文字で示しているのと同じ）。行の右端へ流し、幅が足りなければ末尾から切る |
| Notionの検索条件はSDKの型のまま組み立てる | Notionの複合フィルタは2段までしかネストできず、3段を送ると400（validation_error）が返る。`Record<string, unknown>` で受けて `as never` で流し込んでいたため、「出張 かつ（事前申請が未 または 事後登録が未）」の3段が型検査を素通りして本番まで届き、勤務の画面が丸ごと開けなくなった（issue #402）。3段になる条件は、手続きごとの `and` へ展開して2段に収める。型は `src/services/notion/client.ts` の `NotionQueryFilter` / `NotionFilterGroup` |
| 勤務の画面はNotionの取得に失敗しても開く | 取得の失敗をそのまま投げると、Next.jsの汎用のエラー画面（`This page couldn't load`）へ落ちる。何が起きたのかも、月を送り直せることも画面から分からない。カレンダー（`loadCalendarData()`）とドロワーの未対応件数（`/api/work/alerts`）は既に失敗を受け止めており、勤務の画面だけが500になっていた。握りつぶさず、Notionが返したメッセージを `externalApiMessage()` で画面へ出す |
| 画面が丸ごと描けなかったときの面を `error.tsx` / `global-error.tsx` で持つ | 置くまではNext.jsの汎用のエラー画面（英語の一文）へ落ちていた。何の画面が失敗したのかも、もう一度試せることも、開き直せば直るのかも画面から分からない（issue #402・#407で利用者に見えていたのがこの面）。個々の画面が外部APIの失敗を受け止めるのが先で、ここは漏れたものを受ける最後の一枚。出すのは「一時的かもしれない」「もう一度試せる」「戻れる」とエラーID（`digest`）だけにする。本番のサーバー側の例外は中身がNext.jsに伏せられ、手元にはdigestしか無いため |
| エラーの面が出たページは Service Worker の保存済みから捨てる | 画面を描く途中の例外は、シェルが流れたあとだと HTTP 200 で返る（ルートに `loading.tsx` があるため動的な画面は全てこの形）。Service Workerからは正常なページと区別が付かず `PAGE_CACHE` に入り、次にオフラインになったときや proxy が5xxを返したときに、原因が直ったあとでもその画面だけが古い失敗を再生し続ける。捨てるのは同じURLの1件だけで、クエリ違い（別の月・別の日付）は残す |
| カレンダーの日付の数字も日曜・祝日は赤にする | 曜日の見出し（月表示の7列・時間グリッドの列）だけを塗っても、読んでいるのは個々の日のほう。月表示のマスには曜日が出ないため、数字が平日と同じ色だとその日が休みかどうか数字からは分からない（issue #413）。色は彩度を落とした値のままにする（日付の数字は予定の帯と同じ面に並び、はっきりした赤・青にすると予定の色より前へ出る）。祝日の名前は出さない（月表示は予定の段が1つ減り、時間グリッドはヘッダーが1行ぶん高くなる。名前が要るのは勤務の記録を入れるときで、そちらは勤務の画面の一覧に出ている）。今日の日付は塗りつぶした丸のまま |
| 会社休業日は年休のselectの選択肢にせず専用の checkbox にする | 選択肢の名前に振る舞いを持たせると、Notionで名前を直した瞬間に区分としての扱いが消え、直した本人にも原因が読めない（年休を専用プロパティにしたのと同じ理由）。checkbox は4つに増えるが、対応付けは名前が一致したときだけで、ヒント（会社休業日・休業）は出張・事前申請・事後登録のどれとも重ならない。申請も事後の届け出も無いため未対応の件数には入らず、勤務の画面にも専用の区画を置かない（片付ける手続きが無く、開いて確かめる理由が無い）。名称は任意で、カレンダーのチップには「休業」または名称を出す（タイトルの「会社休業日」のままだと狭い列で「会社」までしか入らない） |
| 買い物リストはNotionの買い物リストDBを一次情報源にする | 機能そのものは別アプリ（shopping-list）に先にあり、同じNotion DBを指せば移行の作業なしにどちらからも同じリストが読める。タスク・日付リマインド・場所・勤務と同じく、利用者が後から見返し・手で直す種類の記録でもある。Googleへ書き出さないのは、日付も時刻も持たずカレンダーに置く場所が無いため |
| 買い物のカテゴリ・優先度は名前が当たったときだけ対応付ける | どちらも select で、型だけを見て空いている欄へ順に割り当てると入れ替わる。入れ替わると「高・中・低」がカテゴリのタブとして並び、売り場の名前が優先度の帯の色を決めることになる。入れ替わったことは画面からは読めない（勤務記録DBの年休・出張と同じ扱い） |
| 買い物の優先度はタスクと同じ行左端の色帯にする | 同じアプリの中で同じ意味（急ぐかどうか）が別の形（shopping-listでは色ドット）で出ると、読むたびに対応づけ直すことになる。色だけに意味を持たせないよう読み上げ用の文字を添えるのも同じ |
| カテゴリ未設定の買い物の項目は「その他」へ束ねる | カテゴリは任意のプロパティで、付けずに足された項目もある。定義済みのカテゴリだけを並べると、その項目がどのタブにも出ない（shopping-listではそうなっていた）。買い物リストは「残っているものが全部見える」ことが前提の一覧 |
| 買い物のカテゴリのタブの並び順はNotionの定義順そのもの | そこが選択肢の一次情報源。DaySpanが別の順を持つと、Notionで並べ替えても画面が追従しない。並び替えは設定 ▸ タグから行い、Notion側の定義順を書き換える |
| 購入済みの切り替えは押した瞬間に画面へ反映する | 買い物中はいちばん押す操作で、Notionへの往復（1秒前後）を待たせると次の棚へ移りながら押せない。失敗したら押す前へ戻し、理由も出す（黙って戻ると、押したはずのチェックが外れた理由が画面から読めない） |
| 買い物の入力は `ItemDialog` に混ぜず専用のダイアログにする | `ItemDialog` は日時の欄が主で、予定・タスク・日付リマインドのどれに切り替えても入力する軸は変わらない。買い物の項目は日付も時刻も持たないため、同じダイアログに入れると切り替えたときに欄がまるごと入れ替わる |
| 買い物のカテゴリ間の横スワイプは持たない | DaySpanの横スワイプ（`use-day-swipe.ts`）は日付列のレイアウトを前提に、ブラウザより先に軸を決め・DOMへ直接書き・速度で送りを判定する組み立てで、一覧へそのままは流用できない。別の作りのスワイプを足すと、同じアプリの中で手応えが2種類になる |
| 買い物画面で選んでいるカテゴリは端末に覚えさせない | 開く理由は「いま何が残っているか」を見ること。前に見ていたカテゴリで開くと、他のカテゴリに残っているものが最初の画面から抜ける。並び順と購入済みの表示は覚える（そちらは見え方の好みで、残りが隠れることはない） |
| 登録した場所の編集は専用画面（`/places`）に置く | 場所は選択肢の定義ではなく、利用者が足していく記録そのもの（買い物・勤務と同じ）。設定 ▸ タグに並べると、Notionのプロパティ定義を直す画面へNotionのページを直す操作が混ざる。入口はドロワーで、勤務・設定とまったく同じ経路にすれば探す場所が1つ増えない |
| 場所の並びはNotionのままにし、絞り込みで探させる | 場所DBが一次情報源で、DaySpanが別の順を持つとNotionで並べ替えても画面が追従しない（買い物のカテゴリのタブと同じ）。名前・住所・タグへの部分一致で足りる。並べ替えの持ち方を1つ増やす理由が無い |
| 場所の住所と地点はまとめて置き換える | 座標があるとき、地図もYahoo!乗換案内も座標のほうを先に見る（`placeCoordinates()`）。住所だけ直して座標が前のまま残ると、画面に出ている文字列と実際に開く地点が食い違う。地図から選び直したときは両方を同時に書く |
| 場所の編集の地図の入口は住所の欄の直下に1つ置く | 住所と地点は常に組で置き換わるため、押す先も1つでよい。地図ボタンを「地点」の区画にだけ置いていたときは、住所を直しに来た操作からは別の欄の付属物にしか見えず、しかも座標のプロパティを持たない場所DBでは入口ごと画面から消えていた（`capabilities.coordinates` で囲っていたため、住所だけのDBでは地図から住所を入れる手段が無かった。issue #452）。出す条件は住所か座標のどちらかがあること。座標を持たないDBでは住所だけが入るので、その旨を欄の下に書き、押した先の地図の文言（タイトル・説明・確定ボタン）も `picks` で実際に保存されるものへ合わせる。地点を保存できない構成で「この地点にする」と出すと、押した先で何が変わるのかを画面が偽ることになる |
| 地図を開くときの中心は `resolveMapStart()` に集約し、場所欄の値で場所DBを引く | 候補から選んだあとの場所欄には `名前 住所` が入る。名前の完全一致だけで照合していたため、登録済みの座標を持つ場所を選んでいるのにその座標が中心にならず、文字列から地点を引き直していた（issue #464）。照合はYahoo!乗換案内の発着地と同じ `matchPlaceByText`（完全一致 → 名前の前方一致）にする。当たった場所に座標が無いときは**住所**を引く。「自宅」「本社」のような名前では地点が引けず、登録されている住所を使わないまま前回の中心か東京駅（`FALLBACK_CENTER`）から始まっていた。場所の編集画面は名前欄と住所欄が分かれているため、引く文字列を `lookupText` で明示して渡す（名前の初期値は名前欄のまま）。関数を画面の外に置くのは、確かめるときに写しを書かずに済ませるため（`resolveYahooPlace()` と同じ） |
| 地点（`lat`/`lon`）と地名（`q`）の取り違えを受け取り側で断つ | `Number(null)` も `Number("")` も 0 になるため、`Number.isFinite(Number(params.get("lat")))` は緯度・経度が付いていない要求でも true になる。`/api/places/geocode` ではこれで `?q=`（地名から地点を引く）の要求がすべて「緯度0・経度0の逆ジオコーディング」として扱われ、Nominatimがギニア湾の海上を該当なしとして返し、画面が `{lat:0,lng:0}` を受け取って地図をそこへ動かしていた（issue #471）。読み取りは `parsePointParams()` に閉じ、両方が値として来ているときだけ地点にする。画面側でも座標として読めない値では動かさない（読めない値で動かすと、選んでいた地点を失う） |
| 住所から地点を引くときは、見つからなければ丁目まで落として引き直す | Nominatimの地名検索は番地の入った日本の住所を引けない（`東京都渋谷区渋谷二丁目21-1`・`大阪府豊中市中桜塚3-1-1` はいずれも該当なし。実測）。DaySpanが組み立てて登録する住所はこの形のため、落とさないと座標を持たない登録済みの場所はどれも中心が決まらない。数字だけの丁目（`中桜塚3-1-1`）は漢数字へ直す（`中桜塚3` のまま引くとNominatimは一丁目を返し、別の丁目が中心になる）。落とす規則は組み立てと同じ `japanese-address.ts` に置き、引き直しは `resolveTextPlace()` に閉じる |
| 場所の編集で開く地図は、保存前のいまの地点を中心にする | `PlaceMapDialog` は名前から地点を引き直す（`/api/places/geocode?q=`）作りで、編集から開くと、すでに地点を持っている場所なのに検索結果の地点にピンが立つ。そのまま「この地点にする」を押した操作が座標を黙って動かす。`initialCenter` を渡された間は地名の検索も現在地の取得も行わない。渡すのは `place.coordinates` ではなく画面の状態のほうで、選び直したあとに開き直すと元の地点へ戻る、という動きにしない |
| 同じ名前への改名は断る | 予定・移動の場所欄はただの文字列で保存され、DaySpanは名前で場所を引いている（`findPlace()` / `placeCoordinates()`、Yahoo!乗換案内の発着地）。同名が2件あるとどちらを指すのか決まらない。画面で弾くだけでなく、サービス層（`PlaceNameTakenError`）でも断る |
| 場所の住所・座標は「消す」と「触らない」を分ける | 座標の欄にDaySpanが読めない文字（`梅田駅の北側` など）が入っていると画面には「地点なし」と出る。それを「消す」として送ると、開いて保存しただけでその文字が消える。APIは項目ごと渡さないことを「触らない」として扱い、画面は触ったときだけ送る |
| 場所の更新・削除はサービス層でも場所DBか確かめる | UIで入口を隠すだけだと、DaySpanのAPIや将来のMCPから直接呼ばれた要求が素通りする。日付リマインドがゴミの日DBを守っているのと同じ形（`PlaceNotEditableError`）。往復は1回増えるが、読み取り側は増えない |
| 日本の住所の番地はOverpassの生タグから組み立てる | Nominatimは日本の住所から街区符号（`addr:block_number`）を落とす。渋谷2-21-1 の地点を逆ジオコーディングしても `house_number` に「1」しか入らず（`extratags` にも出ない）、そのまま繋ぐと「渋谷二丁目1」という実在しない住所になる。街区符号はOSMの生タグには入っており、それを読めるのがOverpass。地図が止まったときにNominatimと並行して1回だけ呼び、120m以内でいちばん近い番地を採る。取れなければ丁目までの住所に落ちるだけで登録は通る（無償の共用サーバーのため、待ちはNominatimと同じ8秒で切る。予備のサーバーはkumi.systems・osm.jp・private.coffeeを試したがいずれも応答しなかった） |
| 街区符号が取れないときは住居番号ごと落とす | 「渋谷二丁目1」のような実在しない住所を作らないため。丁目までの住所は短いが正しく、誤った番地よりGoogleマップ・Yahoo!乗換案内で当たる地点が近い。住居番号の側へ「21-1」の形で街区符号ごと入っているときはそのまま使う |
| 地図の住所は手で直せる欄にする | OSMに番地が登録されていない地点は、どの取得元でも丁目までしか出せない（実測で郊外の住宅地は120m以内に0件）。利用者が番地を書き足せる逃げ道が要る。触ったあとに地図を動かしても上書きしないのは名前欄と同じ扱いで、打った文字列は利用者が決めたものだから |
| 場所の取得は `listPlaces`（投げる）と `loadPlaces`（空で返す）に分ける | 入力の候補として読むだけの経路では、失敗しても空として扱うのが正しい（予定を読むのに要るものではない）。一方、編集画面では「1件も登録が無い」と「取得できなかった」を区別しないと、Notionが落ちている日に「まだ登録していない」画面が出て、直したい場所が消えたように見える |
| 場所のタグは既存の選択肢の仕組み（`TagKind`）へ相乗りさせる | 場所DBのタグはNotionの `multi_select` で、タスクのタグとまったく同じ構造。選択肢の追加・削除・改名・並び替えの手順も同じで、`tag-options.ts` / `/api/notion/tags` / `TagSection` がすでにその全部を持っている。場所のためだけに別の経路を作ると、同じ操作が2か所に増える。書き戻すときの構成（`multi_select` か `select` か）だけが種別で違うため、そこは `MULTI_SELECT_KINDS`（task・place）で分ける |
| 場所のタグは `loadTagCatalog` に含めない | あれはカレンダー・タスク・日付リマインドの各ページが呼んでおり、種別を1つ足すとNotionへの `dataSources.retrieve` がその3画面すべてで1回ずつ増える。場所のタグを使うのは `/places` と 設定 ▸ タグ だけなので、その2画面が1種類ぶんの `loadTagOptions(connection, "place")` を呼ぶ（docs/spec.md §20） |
| 場所のタグは、付け外しは編集ダイアログ・選択肢の管理は設定 ▸ タグ に置く | 「この場所にどのタグを付けるか」は場所という記録そのものの中身で、開いている場所の画面で決まる。一方「どんなタグがあるか・色・並び順・名前」は選択肢の定義で、タスク・日付リマインド・勤務場所・買い物のカテゴリの分が既に設定 ▸ タグ に並んでいる。場所だけを別の画面に置くと、同じ操作が2か所に増える。入力の途中で思いついた名前は `TagPicker` からその場で足せるため、設定画面へ回らされることはない |

## デプロイ

pnpm は 11 系ではなく **10 系** に固定する。VPS の Node.js が 20 系で、pnpm 11 は Node 22.13 以上を
要求する（`node:sqlite` を使うため起動できない）。他アプリと同じ `pnpm@10.34.5` に揃えている。

`deploy.yml` は成果物を `tar` で固める際に `public` を含める。静的ファイルを置いていなくても
ディレクトリ自体が存在しないと `tar` が失敗するため、`public/.gitkeep` を追跡対象に残している。

本番へ渡す値を足すときは、コードと `deploy.yml` だけでなく `.github/secrets-manifest.tsv` と
`docs/setup-checklist.md` にも足し、1Password（正）へ入れてGitHub Secretsへ同期するところまで行う。
GitHub Secretsに無い値は空文字としてワークフローへ渡り、`update_env` はそれをそのまま本番の
`.env` へ書く。起動に要らない値では何も落ちないため、「機能は入っているのに使えない」状態が
気付かれずに残る（通知のVAPID鍵がこれで、v2.1.0のリリース後も未配布のままだった。issue #359）。
デプロイのたびに `secrets-check` ジョブがマニフェストの `repo` 項目と突き合わせ、空のものが
あればSignalyへ知らせる。ただしそれが動くのは `main` へマージしたあとで、値の無いまま
リリースされてからしか気付けない。そのため `ci.yml` にも同じ突き合わせを置き、**develop向けの
PR・pushでは届いていない値があればCIを落とす**（#476）。deploy側は警告のままにする（VAPID鍵の
ように、無くても他の機能は動く値が含まれるため）。

## 外部APIの扱い

- Google Calendar / Notion への呼び出しは `src/services/` を経由し、UIコンポーネントから直接叩かない（`docs/spec.md` §22）。
- 外部APIの失敗を握りつぶさない。`src/lib/api-error.ts` を使い、サーバーログへ全文を残し、画面には外部APIが返したメッセージを表示する。原因が分からないまま「保存できませんでした」だけが出る状態にしない。

## MVP対象外

- 検索
- Push通知 / 期限通知
- MCPサーバー
- リアルタイム同期
- オフライン書き込みと再接続後の同期キュー

## 実装時の参照順

1. この `CLAUDE.md`
2. `docs/spec.md`
3. `.shared-context/CLAUDE.md` または `guchi-apps/docs/CLAUDE.md`
4. 必要な `agent-rules/`、`knowledge/`、`standards/`、`guides/`
5. VPSの現在構成が必要な場合は `guchi-apps/vps`

認証、OAuth、DBスキーマ、Secrets、本番環境設定などの変更では、共通ルールに従って必要なユーザー確認を行うこと。

---

# Issueごとの複数Claude Codeエージェント運用

`@claude` コメントを起点に、計画提示〜実装〜develop向けPR作成〜レビュー〜マージまでをGitHub Actions上で
無人実行する運用を導入している。仕組みの本体は `guchi-apps/issue-deck` にあり、DaySpanはその
再利用可能ワークフロー（`workflows/v9` タグ）を参照する側として構成している。

設計の詳細・各モードの判定ロジックは issue-deck の `docs/multi-agent-workflow.md`・`docs/multi-agent/` を
一次情報源とする。ここにはDaySpan側の運用に必要な事項のみを置く。

## ブランチ運用

- `main` は本番と一致するリリース用ブランチ。直接pushは禁止し、`develop` → `main` のPRのみで進める
- `develop` が日常の開発ブランチ
- Issue専用ブランチは `develop` から作成し、ブランチ名は `issue-<Issue番号>` とする（例: `issue-12`）。
  ラベル遷移・レビュー・コンフリクト解消の各ワークフローはこの命名規約からIssue番号を特定するため、
  従わないブランチはすべて対象外になる
- worktreeは本体リポジトリの外（`~/apps/dayspan-worktrees/<ブランチ名>/`）に作成する。
  本体 `~/apps/dayspan` は `develop` の最新チェックアウトとして空けておく
- 開発サーバーのポートは `scripts/start-issue.sh` が `.env.local` に `PORT=6000 + Issue番号` を設定する
  （例: issue-12 → 6012）。issue-deck（`4000 + Issue番号`）や本番ポート3113・dev既定の3000と重ならない

## Issueの進捗

**進捗はGitHub ProjectsのStatusで管理する。唯一の正はStatusで、進捗ラベルは存在しない**
（guchi-apps/issue-deck#1010 / #991 Phase 5 で `01.planning`〜`09.main` を廃止した）。

原則として以下の順で遷移する。`Planning` は `21.plan-required` が付いている場合のみ経由する。

1. `Ready` — 未着手
2. `Planning` — 計画を検討中
3. `Implementation` — 実装中
4. `Develop PR` — developへPR作成・マージ中
5. `Develop` — developへマージ完了（main未反映）
6. `Release` — mainへのPR作成・マージ中
7. `Done` — mainへマージ完了。**この時点でissueをclose**する

**`gh issue edit` で進捗を進めることはできない。** Statusを書けるのはissue-deckだけで、
ワークフローは進捗報告API（`POST /api/progress`）へ報告する。ブランチのpush・PR作成・PRマージを
トリガーに自動で遷移するため、**エージェントが自分で進捗を動かす必要はない。**
人が動かす場合はissue-deckのカンバンでカードをドラッグするか、画面のボタンを使う。

`00.check-user`（ユーザーの確認・指示が必要）は、上記のどの段階でも他のラベルと併用して付与する。
`00.check-user` を人間が外す操作が「承認」を意味する。

オプション制御のラベル:

| ラベル | 効果 |
|---|---|
| `21.plan-required` | 実装前に計画を提示し、承認を得てから実装に入る |
| `22.merge-confirm-required` | 内容によらず、developへのマージ前に必ず `00.check-user` を付ける |
| `23.preview-required` | PR作成前に開発サーバーの画面で確認し、承認を得る |
| `24.screenshot-required` | PR作成前にスクリーンショットで確認し、承認を得る。**無人実行では現状使えない**（全画面がSupabase Auth + Google OAuthの背後にあり、CIログインバイパスもPlaywright依存も無いため） |
| `11.local` | 付いている間、無人実行ワークフローが計画・実装・分割・追加対応を行わない。ローカルのClaude Codeセッションと二重に進めないための停止フラグ |

進捗の報告は `.github/workflows/issue-labels.yml` が、ブランチpush・PR作成・PRマージを
トリガーに自動で行う。**エージェントが手動で行う作業は無い。**

## 自動マージ不可カテゴリ

以下に該当する変更は、レビュー・統合エージェントが自動マージせず `00.check-user` を付与して
ユーザーの確認を待つ。`claude-review-develop.yml` の `risk-check` ジョブがパスパターンで一次判定し、
パターンに掛からない意味的なリスクはレビューエージェントが二次判定する。

- 認証・認可（`src/proxy.ts`、`src/lib/supabase/**`、`src/app/auth/**`、`src/lib/crypto/**`）
- DBスキーマ変更・マイグレーション（`prisma/migrations/**`）
- 本番環境の設定（`deploy/**`、`**/*.env.tpl`）
- GitHub Actionsやデプロイ設定（`.github/workflows/**`）
- Secretsや環境変数（`.env*`）
- 課金・決済
- 大規模な依存関係の更新（`package.json` のメジャーバージョン更新）
- `develop` → `main` のマージ

無人実行では確認する相手がその場にいないため、**新しい依存関係の追加が必要になった場合は追加せず**、
`00.check-user` を付与して停止する（依存追加はユーザー確認が必須のため）。シークレットの実値も
コミット・コメントのいずれにも書かない。

## 並行Issueの意味的コンフリクト（develop向けPRを出す前に確認する）

develop向けPRのCIは、PRを出した時点の `develop` に取り込んだ結果に対して走る。その後に別のIssueが
developへマージされてもCIは自動では回り直さず、`develop` のブランチ保護も最新化を要求していない
（`required_status_checks.strict` が `false`）。このため、**テキスト上は競合しないのに develop 上で
壊れる変更**が、そのまま自動マージで入る。

実際に issue #241 のPRが、直前にマージされた issue #239 で削除された `ActivityPreset.calendarId` を
使ったまま develop へ入り、develop と v1.0.0 のリリースPRの両方でCIが落ちた（issue #247）。

Prismaスキーマのフィールド削除・関数やエクスポートの改名・型の変更を含むIssueと並行して作業している
場合、PR作成の直前に `git fetch origin && git merge origin/develop`（またはrebase）してから
`pnpm typecheck` を通す。CIの結果だけを根拠にしない。

## develop向けPRは作成直後にマージされうる

`claude-review-develop.yml` はCIが通った時点で自動マージまで進む。issue #352 では、PR作成から
約4分後にレビュー・自動マージが完了し、そのあとに積んだレビュー対応のコミットが取り残された
（マージ済みのPRへpushしても取り込まれない）。

**PRを出したあとに追記が必要になったら、まずPRの状態を確認する。** すでにマージ済みなら、
同じブランチから追加のPRを出す（ブランチは残っているため `gh pr create` がそのまま通る）。
レビュー指摘への対応など、追記が見込まれる場合はPR作成をそこまで待つ。

## 実装エージェントの禁止事項

- `main` / `develop` への直接コミット・push
- 他Issueのブランチ・worktreeの編集
- 不要なforce push
- 自分が作成したPull Requestの自己マージ
- 共有知識リポジトリ（`.shared-context/` / `~/apps/_docs`）の編集・コミット

レビュー・統合エージェントは、加えて `main` への直接マージ・pushを行わない。

## PR本文テンプレート

`develop` 宛のPRには以下を記載する。

- 対応Issue（`closes #番号` / `fixes #番号` は使わず `#番号` のみ。developマージ時点ではissueを
  closeしない運用のため）
- 実装内容
- テスト内容
- 確認方法（画面に関わる変更では `http://localhost:<6000 + Issue番号>` とアクセス手順）
- 注意点

## ローカルからの起動

```bash
scripts/start-issue.sh <issue番号> [issue番号...]
```

worktree作成・`.env.local` のコピー・ポート割り当て・`pnpm install`・開発サーバーの自動起動までを行い、
実装エージェント用のClaude Codeセッションを起動する（プロンプトは `scripts/prompts/implementation-agent.md`）。
Windows側からは `scripts/start-issue.ps1` を使う。

**`.env.local` のコピー元は本体チェックアウト（`~/apps/dayspan`）の `.env.local` で、本体に無ければ
worktreeへは何も配られない**（issue-deck の `supply_env_files` も同じ作り）。配られないまま起動すると
`src/proxy.ts` の `updateSession()` が毎リクエストで `createServerClient()` に落ち、`/login` を含む
全ページが500になる。画面には理由が出ずサーバーログにしか残らないため、確認環境が開かないときは
まず本体の `.env.local` の有無を見る（issue #467）。用意の手順は README「本体チェックアウトの
`.env.local` は worktree の前提」にある。

ローカルセッションで進めるIssueには `11.local` を付け、無人実行と二重に走らないようにする。

## ワークフローの構成

| ファイル | 方式 | 内容 |
|---|---|---|
| `issue-labels.yml` | 参照（`@workflows/v9`） | 進捗（Project Status）の報告 |
| `claude-issue-dispatch.yml` | 参照（`@workflows/v9`） | `@claude` 起点の計画・実装・PR作成 |
| `claude-review-develop.yml` | 参照（`@workflows/v9`） | develop向けPRの自動レビュー・リスク判定・Auto-merge |
| `claude-conflict-resolve.yml` | 参照（`@workflows/v9`） | developとのコンフリクト自動解消 |
| `claude-ci-fix.yml` | 参照（`@workflows/v9`） | CI失敗の自動修正 |
| `release-develop-to-main.yml` | コピー | バージョンbump PR・develop→mainのリリースPR作成 |

**参照しているタグは正ではない。** 上げたらこの表も直すが、実態は
`.github/workflows/` の `uses:` を見るのが確実。

参照方式は `uses:` のタグを上げるだけでissue-deck側の改善が反映される。**`claude-issue-dispatch.yml` は
`uses:` のタグと `prompts-ref` を必ず同じ値にする**（片方だけ上げると新しいワークフローで古い
プロンプトが動く）。コピー方式（`release-develop-to-main.yml` のみ）は移植元コミットをファイル冒頭のコメントに
記録しており、issue-deck側の改善を取り込む際はそこを更新する。

**ただし `on:`（トリガーと `workflow_dispatch` の入力）はcallerが自分で持つコピーで、タグの対象外。**
issue-deck側が新しい入力を渡すようになっても、caller側に同じ入力を定義しなければGitHubが
422（`Unexpected inputs provided`）で起動を拒む。再利用ワークフロー内の `inputs` は
workflow_call のものを指すため、受け取った `workflow_dispatch` 入力は `with:` で明示的に渡す
（`claude-ci-fix.yml` / `claude-conflict-resolve.yml` の `issue-number`）。issue-deckの
`.github/templates/callers/` が正となる形を持つので、そこと見比べる（issue #325）。

無人実行のたびに `.shared-context/`（共有知識）と `.shared-prompts/`（issue-deck側の
実装プロンプト）がワークツリーへcheckoutされる。**どちらもこのリポジトリの管理対象ではない。**
`.gitignore` 済みなので、**編集・`git add`・コミットを一切行わないこと。**

`release-develop-to-main.yml` が生成する利用者向けの更新履歴は、`package.json` の `"version"`
ライフサイクルスクリプト経由で `scripts/update-changelog.mjs` が `src/lib/changelog.ts` へ追記する。
文面のルールは changelog-ja スキルに従う。

同じ経路で**使い方**（`RELEASE_USAGE`）も渡る。「何が変わったか」の `RELEASE_CHANGELOG` に対し、
どこを開く・何を押す・どうなれば成功かを番号付きの複数行で渡すもので、`ChangelogEntry.usage`
（`changes` とは別の項目）として保存し、更新履歴の画面でそのバージョンの変更点の下に出す。
画面で使える変化が無いリリースでは空文字で渡るため、そのときは `usage` の項目ごと持たせない。
共有ワークフロー側の生成は `@workflows/vN` の配布後に有効になる（それまでは常に空文字）。

<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->